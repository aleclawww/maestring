export const CERTIFICATION_ID = 'aws-saa-c03'

export interface ConceptDefinition {
  slug: string
  name: string
  description: string
  domainSlug: string
  topicSlug: string
  difficulty: number // 0-1
  keyFacts: string[]
  examTips: string[]
  awsServices: string[]
  confusedWith: string[]
}

export interface TopicDefinition {
  slug: string
  name: string
  domainSlug: string
}

export interface DomainDefinition {
  slug: string
  name: string
  examWeightPercent: number
  color: string
  description: string
}

export const DOMAINS: DomainDefinition[] = [
  {
    slug: 'resilient-architecture',
    name: 'Resilient Architecture',
    examWeightPercent: 30,
    color: '#6366f1',
    description: 'Design resilient, fault-tolerant multi-tier solutions and disaster recovery',
  },
  {
    slug: 'performant-architecture',
    name: 'High-Performance Architecture',
    examWeightPercent: 28,
    color: '#10b981',
    description: 'Select high-performance storage, compute, network, and DB solutions',
  },
  {
    slug: 'secure-architecture',
    name: 'Secure Architecture',
    examWeightPercent: 24,
    color: '#f59e0b',
    description: 'Design secure access, secure workloads, and data protection',
  },
  {
    slug: 'cost-optimized-architecture',
    name: 'Cost-Optimized Architecture',
    examWeightPercent: 18,
    color: '#ef4444',
    description: 'Identify cost-optimized storage, compute, and service solutions',
  },
]

export const TOPICS: TopicDefinition[] = [
  // Resilient
  { slug: 'ec2-compute', name: 'EC2 & Compute', domainSlug: 'resilient-architecture' },
  { slug: 'storage-resilience', name: 'Resilient Storage', domainSlug: 'resilient-architecture' },
  { slug: 'database-resilience', name: 'Resilient Databases', domainSlug: 'resilient-architecture' },
  { slug: 'networking-connectivity', name: 'Networking and Connectivity', domainSlug: 'resilient-architecture' },
  // Performant
  { slug: 'content-delivery', name: 'Content Delivery', domainSlug: 'performant-architecture' },
  { slug: 'messaging-integration', name: 'Messaging and Integration', domainSlug: 'performant-architecture' },
  { slug: 'containers-serverless', name: 'Containers and Serverless', domainSlug: 'performant-architecture' },
  { slug: 'performance-database', name: 'High-Performance DB', domainSlug: 'performant-architecture' },
  { slug: 'data-analytics', name: 'Analytics and Data Lake', domainSlug: 'performant-architecture' },
  // Secure
  { slug: 'iam-identity', name: 'IAM and Identity', domainSlug: 'secure-architecture' },
  { slug: 'data-protection', name: 'Data Protection', domainSlug: 'secure-architecture' },
  { slug: 'network-security', name: 'Network Security', domainSlug: 'secure-architecture' },
  // Cost Optimized
  { slug: 'cost-compute', name: 'Cost-Optimized Compute', domainSlug: 'cost-optimized-architecture' },
  { slug: 'cost-storage', name: 'Cost-Optimized Storage', domainSlug: 'cost-optimized-architecture' },
  { slug: 'cost-governance', name: 'Cost Governance', domainSlug: 'cost-optimized-architecture' },
]

export const CONCEPTS: ConceptDefinition[] = [
  // ---- EC2 ----
  {
    slug: 'ec2-instance-types',
    name: 'EC2 Instance Types',
    description: 'EC2 instance families and when to use each one (General Purpose, Compute Optimized, Memory Optimized, Storage Optimized, Accelerated Computing)',
    domainSlug: 'resilient-architecture',
    topicSlug: 'ec2-compute',
    difficulty: 0.4,
    keyFacts: [
      'T family: burstable, baseline CPU + burst credits — ideal for sporadic workloads',
      'M family (General Purpose): balanced CPU/memory — webservers, app servers',
      'C family (Compute Optimized): high CPU/memory ratio — HPC, batch, ML inference',
      'R/X/Z family (Memory Optimized): large in-memory databases, SAP HANA',
      'I/D family (Storage Optimized): NVMe local SSD, high IOPS — NoSQL databases',
      'P/G/Inf family (Accelerated): GPUs for ML training, video encoding',
    ],
    examTips: [
      'If it says "intermittent workloads" → T family with unlimited credits OFF',
      'If it says "lowest latency" with local storage → Instance store, not EBS',
      'HPC always → C family + cluster placement group',
    ],
    awsServices: ['EC2', 'Auto Scaling'],
    confusedWith: ['ec2-pricing-models'],
  },
  {
    slug: 'ec2-pricing-models',
    name: 'EC2 Pricing Models',
    description: 'On-Demand, Reserved (Standard/Convertible), Spot, Dedicated Hosts and Savings Plans',
    domainSlug: 'cost-optimized-architecture',
    topicSlug: 'cost-compute',
    difficulty: 0.5,
    keyFacts: [
      'On-Demand: pay per hour/second, no commitment — baseline',
      'Reserved Standard 1y/3y: up to 72% discount, specific AZ or Region',
      'Reserved Convertible: up to 54% discount, can change family/OS',
      'Spot: up to 90% discount, can be interrupted with 2min notice',
      'Dedicated Host: dedicated physical server — BYOL licensing (Oracle, Windows)',
      'Savings Plans: $/hour commitment for 1-3 years, more flexible than Reserved',
    ],
    examTips: [
      'Spot → fault-tolerant workloads, batch, data analysis, CI/CD',
      'Dedicated Host → compliance, socket/core licensing (Oracle DB)',
      'Savings Plans > Reserved Convertible in flexibility',
    ],
    awsServices: ['EC2', 'Savings Plans', 'Cost Explorer'],
    confusedWith: ['ec2-instance-types'],
  },
  {
    slug: 'ec2-auto-scaling',
    name: 'Auto Scaling Groups (ASG)',
    description: 'Automatic scaling of EC2 instances based on policies, schedules, and metrics',
    domainSlug: 'resilient-architecture',
    topicSlug: 'ec2-compute',
    difficulty: 0.5,
    keyFacts: [
      'Launch Template replaces Launch Configuration (more features)',
      'Scaling policies: Target Tracking (simplest), Step Scaling, Simple Scaling',
      'Target Tracking with CPU 50% → the most recommended on the exam',
      'Cooldown period: prevents double-scaling (default 300s)',
      'Health checks: EC2 (default) or ELB — ELB is more comprehensive',
      'Lifecycle hooks: pause instances on launch/terminate for custom logic',
    ],
    examTips: [
      'For "scale out fast" → Step Scaling or Target Tracking with aggressive alarms',
      '"Replace unhealthy instances" → always ASG with ELB health checks',
      'Predictive Scaling uses ML — for workloads with regular patterns',
    ],
    awsServices: ['EC2 Auto Scaling', 'ELB', 'CloudWatch'],
    confusedWith: ['elb-types'],
  },
  {
    slug: 'ec2-placement-groups',
    name: 'EC2 Placement Groups',
    description: 'Cluster, Spread, and Partition — physical placement strategies for EC2',
    domainSlug: 'performant-architecture',
    topicSlug: 'ec2-compute',
    difficulty: 0.6,
    keyFacts: [
      'Cluster: same rack/AZ — ultra-low latency, 10Gbps+ network — ONLY ONE AZ',
      'Spread: instances on different racks — max 7 per AZ — critical high availability',
      'Partition: groups of instances in isolated partitions — Hadoop, Kafka, Cassandra',
      'Cluster has HIGH risk of AZ failure, Spread is for maximum resilience',
    ],
    examTips: [
      'HPC + MPI + "lowest latency" → Cluster placement group',
      '"Cannot tolerate correlated hardware failure" → Spread',
      '"Distributed big data with rack awareness" → Partition',
    ],
    awsServices: ['EC2'],
    confusedWith: ['ec2-auto-scaling'],
  },
  // ---- S3 ----
  {
    slug: 's3-storage-classes',
    name: 'S3 Storage Classes',
    description: 'Standard, IA, One Zone-IA, Glacier, Deep Archive and their cost/availability trade-offs',
    domainSlug: 'cost-optimized-architecture',
    topicSlug: 'cost-storage',
    difficulty: 0.5,
    keyFacts: [
      'Standard: 99.99% availability, 3+ AZ, instant access — frequently accessed data',
      'Standard-IA: 99.9% availability, 3 AZ, retrieval fee — infrequent data (>30 days)',
      'One Zone-IA: 99.5% availability, 1 AZ, 20% cheaper — reproducible data',
      'Intelligent-Tiering: automatically moves between tiers based on access',
      'Glacier Instant: millisecond access — archiving with occasional access',
      'Glacier Flexible: 1-12 hour retrieval — bulk option, cheaper',
      'Glacier Deep Archive: 12-48 hours — cheapest in S3, 7-10 year retention',
    ],
    examTips: [
      'Minimum storage duration: Standard-IA 30 days, Glacier 90 days, Deep Archive 180 days',
      '"Infrequently accessed + available immediately" → Standard-IA or Glacier Instant',
      '"Compliance + 7 years" → Deep Archive with Vault Lock',
    ],
    awsServices: ['S3', 'S3 Glacier'],
    confusedWith: ['s3-lifecycle'],
  },
  {
    slug: 's3-replication',
    name: 'S3 Replication (CRR and SRR)',
    description: 'Cross-Region Replication and Same-Region Replication for compliance and DR',
    domainSlug: 'resilient-architecture',
    topicSlug: 'storage-resilience',
    difficulty: 0.6,
    keyFacts: [
      'CRR (Cross-Region): compliance, low latency for global users, DR',
      'SRR (Same-Region): log aggregation, prod→staging replication in same region',
      'Versioning REQUIRED on source and destination buckets',
      'Only replicates NEW objects after enabling (not retroactive without Batch Replication)',
      'Delete markers are NOT replicated by default (configurable)',
      'Replication Time Control (RTC): SLA 99.99% within 15 minutes',
    ],
    examTips: [
      '"DR in another region" → CRR + RTC for guaranteed time',
      '"Centralized audit logs" → SRR to aggregate multiple buckets',
      'Objects encrypted with SSE-KMS require extra key policy configuration',
    ],
    awsServices: ['S3', 'KMS'],
    confusedWith: ['s3-lifecycle'],
  },
  {
    slug: 's3-lifecycle',
    name: 'S3 Lifecycle Policies',
    description: 'Rules to automatically transition and expire objects between storage classes',
    domainSlug: 'cost-optimized-architecture',
    topicSlug: 'cost-storage',
    difficulty: 0.4,
    keyFacts: [
      'Transition actions: move objects to another class after N days',
      'Expiration actions: delete objects or non-current versions',
      'Minimum 30 days in Standard before transitioning to Standard-IA or One Zone-IA',
      'S3 Analytics helps identify access patterns to configure lifecycle',
      'You can filter by prefix, tags, or object size',
    ],
    examTips: [
      '"Move to Glacier after 90 days" → Lifecycle rule: Transition to Glacier after 90d',
      '"Delete incomplete multipart uploads" → Lifecycle rule with AbortIncompleteMultipartUpload',
      'You can combine CRR + Lifecycle on the same bucket',
    ],
    awsServices: ['S3'],
    confusedWith: ['s3-storage-classes', 's3-replication'],
  },
  {
    slug: 's3-security',
    name: 'S3 Security',
    description: 'Bucket policies, ACLs, Block Public Access, SSE-S3/SSE-KMS/SSE-C/DSSE-KMS encryption',
    domainSlug: 'secure-architecture',
    topicSlug: 'data-protection',
    difficulty: 0.6,
    keyFacts: [
      'Block Public Access: account and bucket setting — takes precedence over bucket policy',
      'Bucket Policy: resource-based, allows/denies granular access by ARN',
      'SSE-S3: AWS manages keys (AES-256), no extra cost',
      'SSE-KMS: keys in KMS, auditable, more expensive (API calls)',
      'SSE-C: client provides and manages the key',
      'DSSE-KMS: dual-layer KMS encryption — stricter security requirements',
      'S3 Object Lock: WORM — governance mode vs compliance mode',
    ],
    examTips: [
      '"Audit who accessed key" → SSE-KMS (CloudTrail integrated)',
      '"Client manages key outside AWS" → SSE-C',
      '"Prevent deletion for compliance" → S3 Object Lock + Compliance mode',
      'MFA Delete: requires MFA to delete versions or disable versioning',
    ],
    awsServices: ['S3', 'KMS', 'CloudTrail', 'IAM'],
    confusedWith: ['kms-encryption', 'iam-fundamentals'],
  },
  // ---- RDS ----
  {
    slug: 'rds-multi-az',
    name: 'RDS Multi-AZ',
    description: 'Synchronous replication to a standby AZ for high availability — automatic failover',
    domainSlug: 'resilient-architecture',
    topicSlug: 'database-resilience',
    difficulty: 0.5,
    keyFacts: [
      'SYNCHRONOUS replication to the standby in another AZ',
      'Automatic failover in 1-2 minutes (CNAME flip)',
      'The standby does NOT serve read traffic (Multi-AZ ≠ Read Replica)',
      'Multi-AZ Multi-Standby (new): up to 3 standbys in different AZs',
      'Backup is taken from the standby to avoid impacting production',
      'Supported on: MySQL, PostgreSQL, Oracle, SQL Server, MariaDB',
    ],
    examTips: [
      '"High availability" + "automatic failover" → Multi-AZ',
      '"Scale read traffic" → Read Replicas (not Multi-AZ)',
      '"Zero RPO" → Multi-AZ (synchronous)',
      'Multi-AZ does not prevent data loss from logical errors (bugs, accidental DELETE)',
    ],
    awsServices: ['RDS', 'Route 53'],
    confusedWith: ['rds-read-replicas', 'aurora-global'],
  },
  {
    slug: 'rds-read-replicas',
    name: 'RDS Read Replicas',
    description: 'Asynchronous read-only replicas to scale queries and reduce load on the primary',
    domainSlug: 'performant-architecture',
    topicSlug: 'performance-database',
    difficulty: 0.5,
    keyFacts: [
      'ASYNCHRONOUS replication — there can be lag (eventual consistency)',
      'Up to 15 replicas (Aurora) or 5 (RDS)',
      'Can be in another region (Cross-Region Read Replica)',
      'Can be promoted to standalone instance (useful for DR)',
      'Different connection endpoint — application must route explicitly',
      'For MySQL/PostgreSQL, cross-region incurs data transfer cost',
    ],
    examTips: [
      '"Reporting queries slow down production" → Read Replica for reporting',
      '"RPO minutes" in another region → Cross-Region Read Replica',
      'For analytics-heavy → Redshift, not Read Replica',
    ],
    awsServices: ['RDS', 'Aurora'],
    confusedWith: ['rds-multi-az', 'aurora-global'],
  },
  {
    slug: 'aurora-global',
    name: 'Aurora Global Database',
    description: 'Global Aurora replication with < 1 second lag for active-passive DR',
    domainSlug: 'resilient-architecture',
    topicSlug: 'database-resilience',
    difficulty: 0.7,
    keyFacts: [
      '1 primary region (writes), up to 5 secondary regions (reads)',
      'Typical lag < 1 second between regions',
      'Manual failover to secondary in < 1 minute (Managed Planned Failover)',
      'Write Forwarding: replicas can forward writes to the primary',
      'Up to 16 Read Replicas per secondary region',
      'RPO 1 second, RTO < 1 minute with Managed Failover',
    ],
    examTips: [
      '"Global users low latency reads" → Aurora Global',
      '"Sub-second RPO cross-region" → Aurora Global vs RDS Cross-Region (lag in seconds)',
      '"CRDR" (Cross-Region Disaster Recovery) → Aurora Global Database',
    ],
    awsServices: ['Aurora', 'Route 53'],
    confusedWith: ['rds-read-replicas', 'rds-multi-az'],
  },
  {
    slug: 'elasticache',
    name: 'ElastiCache (Redis and Memcached)',
    description: 'In-memory cache to reduce DB latency and improve application performance',
    domainSlug: 'performant-architecture',
    topicSlug: 'performance-database',
    difficulty: 0.6,
    keyFacts: [
      'Redis: optional persistence, complex data structures, pub/sub, Multi-AZ, clustering',
      'Memcached: pure cache, multi-threaded, no persistence, no replication',
      'Lazy Loading: cache miss → reads from DB → loads cache',
      'Write Through: writes to DB and cache simultaneously',
      'Redis Cluster: automatic sharding, up to 500 nodes',
      'Redis AUTH + TLS in transit, encryption at rest',
    ],
    examTips: [
      '"Session store" → Redis (persistence)',
      '"Simple stateless caching" → Memcached (cheaper)',
      '"Leaderboard, sorted sets" → Redis ZSET',
      '"RDS is slow" → ElastiCache between app and RDS',
    ],
    awsServices: ['ElastiCache', 'RDS', 'DynamoDB'],
    confusedWith: ['rds-multi-az'],
  },
  // ---- VPC ----
  {
    slug: 'vpc-fundamentals',
    name: 'VPC Fundamentals',
    description: 'Public/private subnets, Internet Gateway, NAT Gateway, Route Tables, and NACLs vs Security Groups',
    domainSlug: 'secure-architecture',
    topicSlug: 'network-security',
    difficulty: 0.5,
    keyFacts: [
      'CIDR /16 to /28 — /16 is the largest (65,534 usable IPs)',
      'AWS reserves 5 IPs per subnet: .0 network, .1 router, .2 DNS, .3 future, last broadcast',
      'Internet Gateway: allows internet access, stateful for instances with public IP',
      'NAT Gateway: allows outbound from private subnets without exposing private IPs',
      'Security Groups: stateful, allow rules only, applies to instances',
      'NACLs: stateless, allow + deny, applies to subnets, evaluated in order',
      'Flow Logs: captures IP traffic metadata for analysis and troubleshooting',
    ],
    examTips: [
      '"Block specific IP" → NACL (deny rule), Security Groups have no deny',
      '"Private subnet accesses internet" → NAT Gateway (not Internet Gateway)',
      '"Redundant NAT" → NAT Gateway per AZ (not automatically cross-AZ)',
    ],
    awsServices: ['VPC', 'NAT Gateway', 'Internet Gateway', 'Flow Logs'],
    confusedWith: ['vpc-endpoints', 'transit-gateway'],
  },
  {
    slug: 'vpc-endpoints',
    name: 'VPC Endpoints (Gateway and Interface)',
    description: 'Private connection to AWS services without traversing the public internet',
    domainSlug: 'secure-architecture',
    topicSlug: 'network-security',
    difficulty: 0.7,
    keyFacts: [
      'Gateway Endpoint: ONLY S3 and DynamoDB, free of charge, modifies Route Table',
      'Interface Endpoint (PrivateLink): nearly all services, hourly + GB cost, creates an ENI',
      'Gateway Endpoint does not require DNS, Interface Endpoint does (Private DNS)',
      'Endpoint Policy: controls which actions can be performed on the endpoint',
      'Interface Endpoint available in multiple AZs for HA',
    ],
    examTips: [
      '"S3 from private subnet without internet" → Gateway Endpoint (free)',
      '"KMS from private subnet" → Interface Endpoint (PrivateLink)',
      '"Avoid NAT Gateway cost for S3" → S3 Gateway Endpoint',
    ],
    awsServices: ['VPC', 'S3', 'DynamoDB', 'PrivateLink'],
    confusedWith: ['vpc-fundamentals', 'vpc-peering'],
  },
  {
    slug: 'vpc-peering',
    name: 'VPC Peering',
    description: 'Point-to-point connection between two VPCs with private routing, not transitive',
    domainSlug: 'resilient-architecture',
    topicSlug: 'networking-connectivity',
    difficulty: 0.6,
    keyFacts: [
      'It is NOT transitive: A-B and B-C peered, but A cannot talk to C',
      'CIDRs cannot overlap',
      'Works inter-region and cross-account',
      'Requires updating Route Tables in both VPCs',
      'No bandwidth limit (uses AWS backbone)',
      'With many VPCs → Transit Gateway is more scalable than full-mesh peering',
    ],
    examTips: [
      '"5 VPCs interconnected" → Transit Gateway > full-mesh peering (N²/2 connections)',
      '"Transitive routing" → NOT possible with VPC Peering',
      '"Share services to many VPCs" → PrivateLink (more secure than peering)',
    ],
    awsServices: ['VPC', 'Transit Gateway'],
    confusedWith: ['transit-gateway', 'direct-connect'],
  },
  {
    slug: 'transit-gateway',
    name: 'Transit Gateway (TGW)',
    description: 'Central hub to connect multiple VPCs, accounts, and on-premises networks',
    domainSlug: 'resilient-architecture',
    topicSlug: 'networking-connectivity',
    difficulty: 0.7,
    keyFacts: [
      'Hub and spoke: replaces full-mesh VPC Peering',
      'Supports transitive routing (unlike VPC Peering)',
      'Can connect Direct Connect, Site-to-Site VPN, and VPCs',
      'Transit Gateway Peering: connect TGWs in different regions',
      'Route Tables in TGW: control what can see each other',
      'Multicast support: useful for financial streaming',
    ],
    examTips: [
      '"Multiple VPCs + on-premises + centralized routing" → Transit Gateway',
      '"Shared services VPC (security, DNS)" → TGW with attachment',
      'TGW has cost per attachment + per GB transferred',
    ],
    awsServices: ['Transit Gateway', 'VPC', 'Direct Connect', 'VPN'],
    confusedWith: ['vpc-peering', 'direct-connect'],
  },
  {
    slug: 'direct-connect',
    name: 'AWS Direct Connect',
    description: 'Dedicated private network connection from on-premises to AWS, low latency and consistent bandwidth',
    domainSlug: 'resilient-architecture',
    topicSlug: 'networking-connectivity',
    difficulty: 0.7,
    keyFacts: [
      'Dedicated physical connection, 1 Gbps to 100 Gbps',
      'Does not traverse the public internet — predictable latency',
      'Direct Connect Gateway: connects the same DX to multiple regions/VPCs',
      'Setup takes weeks (physical provisioning)',
      'For DX backup use Site-to-Site VPN (cheaper)',
      'Hosted Connection: shared (50 Mbps - 10 Gbps) via APN partners',
    ],
    examTips: [
      '"Consistent bandwidth, low latency to on-premises" → Direct Connect',
      '"DX redundancy" → second DX circuit or Site-to-Site VPN as backup',
      '"Immediate connectivity" → VPN (DX takes weeks)',
    ],
    awsServices: ['Direct Connect', 'VPN', 'Transit Gateway'],
    confusedWith: ['transit-gateway', 'vpc-peering'],
  },
  // ---- IAM ----
  {
    slug: 'iam-fundamentals',
    name: 'IAM Fundamentals',
    description: 'Users, Groups, Roles, Policies (Identity vs Resource based) and the principle of least privilege',
    domainSlug: 'secure-architecture',
    topicSlug: 'iam-identity',
    difficulty: 0.4,
    keyFacts: [
      'Identity-based policies: attached to users/groups/roles',
      'Resource-based policies: attached to the resource (S3 bucket policy, SQS queue policy)',
      'Evaluation logic: explicit DENY > explicit ALLOW > implicit DENY',
      'IAM Roles for services: EC2 instance profile, Lambda execution role',
      'Conditions: allow access based on IP, MFA, time, tags',
      'Permissions Boundary: maximum permission ceiling (does not grant permissions on its own)',
    ],
    examTips: [
      '"Cross-account access" → IAM Role with trust policy (not users)',
      '"Service needs to access S3" → IAM Role (no hardcoded access keys)',
      'Explicit Deny always wins over any Allow',
    ],
    awsServices: ['IAM', 'STS', 'Organizations'],
    confusedWith: ['iam-roles-vs-policies', 'scp-organizations'],
  },
  {
    slug: 'iam-roles-vs-policies',
    name: 'IAM Roles vs Policies',
    description: 'When to use Roles vs Policies, assume role, trust policies, and cross-account access patterns',
    domainSlug: 'secure-architecture',
    topicSlug: 'iam-identity',
    difficulty: 0.6,
    keyFacts: [
      'Role: temporary identity with trust policy + permission policy',
      'AssumeRole via STS: returns temporary credentials (Access Key + Secret + Token)',
      'Instance Profile: wrapper to attach a role to EC2',
      'Cross-account: Account A assumes role in Account B via STS',
      'External ID: for confused deputy problem cases in third-party',
      'Session tags: additional context in the temporary credentials',
    ],
    examTips: [
      '"On-premises needs access to AWS" → IAM Role + AssumeRole (not IAM users)',
      '"Third party integration" → Role with External ID (confused deputy)',
      '"Temporary credentials" → STS AssumeRole',
    ],
    awsServices: ['IAM', 'STS', 'EC2 Instance Profile'],
    confusedWith: ['iam-fundamentals', 'cognito'],
  },
  {
    slug: 'scp-organizations',
    name: 'SCPs and AWS Organizations',
    description: 'Service Control Policies as preventive guardrails in multi-account setup',
    domainSlug: 'secure-architecture',
    topicSlug: 'iam-identity',
    difficulty: 0.7,
    keyFacts: [
      'SCP does not grant permissions, only limits the maximum possible in the account',
      'Applies to all principals EXCEPT the Management Account',
      'SCPs + IAM policies: the effective permission is the intersection',
      'Inheritance: SCP applies to the OU and all its children (accounts)',
      'Use cases: deny region change, deny leaving Org, deny creating IAM users',
      'Service Control Policies (preventive) vs Config Rules (detective)',
    ],
    examTips: [
      '"Prevent any user including admin from disabling CloudTrail" → SCP (not IAM)',
      '"Restrict to approved regions" → SCP DenyAllOutside...',
      'SCP does not apply to the Management Account — use with care',
    ],
    awsServices: ['AWS Organizations', 'SCP', 'CloudTrail', 'Config'],
    confusedWith: ['iam-fundamentals', 'iam-roles-vs-policies'],
  },
  {
    slug: 'cognito',
    name: 'Amazon Cognito',
    description: 'User Pools (authentication) and Identity Pools (AWS authorization) for web/mobile applications',
    domainSlug: 'secure-architecture',
    topicSlug: 'iam-identity',
    difficulty: 0.7,
    keyFacts: [
      'User Pool: user directory (signup/signin, MFA, social login)',
      'Identity Pool (Federated Identities): exchanges User Pool token for temporary AWS credentials',
      'User Pool returns JWT tokens (ID, Access, Refresh)',
      'Identity Pool internally uses STS AssumeRoleWithWebIdentity',
      'Can federate with SAML 2.0, OIDC providers, social (Google, Facebook)',
      'Hosted UI: ready-to-use login page, customizable',
    ],
    examTips: [
      '"Mobile app users access S3 directly" → Cognito Identity Pool (temporary credentials)',
      '"Web app login/signup" → Cognito User Pool',
      '"SSO with corporate AD" → User Pool + SAML IdP',
    ],
    awsServices: ['Cognito', 'IAM', 'STS', 'API Gateway'],
    confusedWith: ['iam-roles-vs-policies'],
  },
  // ---- Security ----
  {
    slug: 'kms-encryption',
    name: 'AWS KMS',
    description: 'Encryption key management, Customer Managed Keys vs AWS Managed Keys, KMS envelope encryption',
    domainSlug: 'secure-architecture',
    topicSlug: 'data-protection',
    difficulty: 0.7,
    keyFacts: [
      'Customer Managed Key (CMK): full control over rotation, policy, deletion (7-30 day pending)',
      'AWS Managed Key: managed by AWS for the service, automatic annual rotation',
      'AWS Owned Key: AWS internal, no customer visibility',
      'Envelope Encryption: KMS encrypts a Data Encryption Key (DEK), DEK encrypts data',
      'KMS has a 4KB limit per encrypt/decrypt — for large data use envelope',
      'Multi-Region Keys: replicate CMK to other regions for DR/global use',
    ],
    examTips: [
      '"Audit every key usage" → CMK + CloudTrail',
      '"Encrypt > 4KB with KMS" → GenerateDataKey + envelope encryption',
      '"Copy encrypted AMI cross-region" → Multi-Region Key or re-encrypt at destination',
    ],
    awsServices: ['KMS', 'CloudTrail', 'S3', 'EBS', 'RDS'],
    confusedWith: ['secrets-manager', 's3-security'],
  },
  {
    slug: 'secrets-manager',
    name: 'AWS Secrets Manager',
    description: 'Secure storage and automatic rotation of secrets, DB credentials, and API keys',
    domainSlug: 'secure-architecture',
    topicSlug: 'data-protection',
    difficulty: 0.5,
    keyFacts: [
      'Automatic rotation with Lambda — natively for RDS, Redshift, DocumentDB',
      'Cross-account access with resource-based policy',
      'KMS encryption is required',
      'Parameter Store (SSM): cheaper, no native automatic rotation',
      'Secrets Manager: $0.40/secret/month — more expensive but more features',
      'Client-side caching to reduce API calls',
    ],
    examTips: [
      '"Automatic rotation of DB credentials" → Secrets Manager (not Parameter Store)',
      '"Store configuration parameters" → Parameter Store (cheaper)',
      '"Share secrets cross-account" → Secrets Manager with resource policy',
    ],
    awsServices: ['Secrets Manager', 'KMS', 'Lambda', 'RDS'],
    confusedWith: ['kms-encryption'],
  },
  {
    slug: 'shield-waf',
    name: 'AWS Shield and WAF',
    description: 'Shield Standard/Advanced for DDoS, WAF for HTTP/HTTPS traffic filtering at L7',
    domainSlug: 'secure-architecture',
    topicSlug: 'network-security',
    difficulty: 0.6,
    keyFacts: [
      'Shield Standard: free, automatic for everyone — protects against L3/L4 DDoS',
      'Shield Advanced: $3,000/month, protects CloudFront, ALB, ELB, EC2, R53 — DRT team',
      'WAF: rules to block SQL injection, XSS, rate limiting, geo-blocking',
      'WAF WebACL: can be associated with CloudFront, ALB, API Gateway, AppSync',
      'AWS Managed Rules: OWASP Top 10, Amazon IP Reputation, Core Rule Set',
      'WAF + Shield Advanced: complete L3-L7 protection',
    ],
    examTips: [
      '"Protect against OWASP Top 10" → WAF with Managed Rules',
      '"DDoS protection with cost protection" → Shield Advanced',
      '"Block requests from specific country" → WAF geo-match rule',
    ],
    awsServices: ['Shield', 'WAF', 'CloudFront', 'ALB'],
    confusedWith: ['vpc-fundamentals', 'security-hub'],
  },
  {
    slug: 'security-hub',
    name: 'AWS Security Hub',
    description: 'Centralized view of security findings from multiple AWS services and compliance standards',
    domainSlug: 'secure-architecture',
    topicSlug: 'network-security',
    difficulty: 0.5,
    keyFacts: [
      'Aggregates findings from: GuardDuty, Inspector, Macie, Firewall Manager, Config',
      'Standards: AWS Foundational Security, CIS, PCI DSS',
      'ASFF (Amazon Security Finding Format): normalized format',
      'Automated response: EventBridge → Lambda for remediation',
      'Multi-account via Organizations: management account sees everything',
    ],
    examTips: [
      '"Centralized security findings across accounts" → Security Hub',
      '"Detect sensitive data in S3" → Macie (then Security Hub aggregates)',
      '"Automated remediation of security findings" → Security Hub + EventBridge + Lambda',
    ],
    awsServices: ['Security Hub', 'GuardDuty', 'Macie', 'Inspector', 'Config'],
    confusedWith: ['shield-waf'],
  },
  // ---- Content Delivery ----
  {
    slug: 'cloudfront-caching',
    name: 'CloudFront and Caching',
    description: 'Global CDN, edge caching, Origins, Behaviors, Cache Policies, and Lambda@Edge',
    domainSlug: 'performant-architecture',
    topicSlug: 'content-delivery',
    difficulty: 0.6,
    keyFacts: [
      '400+ global edge locations — reduces latency by serving from the edge',
      'Origin: S3, ALB, EC2, custom HTTP — with Origin Access Control (OAC) for S3',
      'Cache Policies: control TTL, headers, and query strings included in the cache key',
      'Origin Request Policies: what is forwarded to the origin without being included in the cache key',
      'Lambda@Edge: runs Lambda at the edge (us-east-1) — A/B testing, auth, rewrites',
      'CloudFront Functions: cheaper, more limited — simple header manipulation',
      'Signed URLs/Cookies: access to restricted content from CloudFront',
    ],
    examTips: [
      '"Global static content" → CloudFront + S3 with OAC',
      '"Restrict S3 access only via CloudFront" → OAC + S3 bucket policy',
      '"Custom auth at edge" → Lambda@Edge (not CloudFront Functions for complex logic)',
    ],
    awsServices: ['CloudFront', 'S3', 'Lambda@Edge', 'ACM'],
    confusedWith: ['route53-routing', 'api-gateway'],
  },
  {
    slug: 'route53-routing',
    name: 'Route 53 Routing Policies',
    description: 'Simple, Weighted, Latency, Failover, Geolocation, Geoproximity, and Multivalue routing',
    domainSlug: 'performant-architecture',
    topicSlug: 'content-delivery',
    difficulty: 0.7,
    keyFacts: [
      'Simple: one record, one value — no health checks for routing decisions',
      'Weighted: distribute traffic by percentage (A/B deployments)',
      'Latency: redirects to the endpoint with the lowest latency measured from AWS',
      'Failover: Active/Passive with required health checks',
      'Geolocation: based on the user\'s geographic location (compliance)',
      'Geoproximity: like geolocation but with bias to adjust regions',
      'Multivalue: like simple but with health checks, up to 8 values',
    ],
    examTips: [
      '"Gradual blue-green deployment" → Weighted (100%→0 blue, 0%→100 green)',
      '"Serve legal content by country" → Geolocation',
      '"Active-passive DR" → Failover routing with health checks',
      '"Route to multiple IPs with health checks" → Multivalue (not simple)',
    ],
    awsServices: ['Route 53', 'CloudFront', 'ELB'],
    confusedWith: ['elb-types', 'cloudfront-caching'],
  },
  {
    slug: 'api-gateway',
    name: 'Amazon API Gateway',
    description: 'REST, HTTP, and WebSocket APIs — throttling, caching, authorizers, and VPC Link',
    domainSlug: 'performant-architecture',
    topicSlug: 'content-delivery',
    difficulty: 0.6,
    keyFacts: [
      'REST API: more features (usage plans, API keys, custom authorizers)',
      'HTTP API: cheaper (70%), simpler — does not natively support API keys',
      'WebSocket API: persistent bidirectional communication',
      'Throttling: 10,000 RPS default per account (adjustable)',
      'Caching: configurable TTL, customizable key',
      'Authorizers: Lambda (custom), Cognito User Pool, JWT',
      'VPC Link: connect API Gateway to private resources in VPC (ALB, NLB)',
    ],
    examTips: [
      '"Serverless API with auth" → API Gateway + Lambda + Cognito authorizer',
      '"Private ALB exposed as API" → API Gateway + VPC Link',
      '"Cost-optimize API" → HTTP API (not REST) if you do not need usage plans',
    ],
    awsServices: ['API Gateway', 'Lambda', 'Cognito', 'VPC'],
    confusedWith: ['cloudfront-caching', 'elb-types'],
  },
  {
    slug: 'elb-types',
    name: 'Elastic Load Balancing (ALB, NLB, GWLB)',
    description: 'Application (L7), Network (L4), and Gateway Load Balancer — when to use each',
    domainSlug: 'resilient-architecture',
    topicSlug: 'ec2-compute',
    difficulty: 0.6,
    keyFacts: [
      'ALB (L7): HTTP/HTTPS, path-based routing, host-based routing, WebSocket, gRPC',
      'NLB (L4): TCP/UDP, ultra-low latency (<1ms), static IP, Elastic IP',
      'GWLB (L3): transparent for network appliances (firewalls, IDS/IPS) — GENEVE protocol',
      'ALB + NLB = Application load balancing + static IP',
      'Cross-Zone Load Balancing: distributes between AZs (ALB always ON, NLB charges extra)',
      'Access Logs + CloudWatch metrics for monitoring',
    ],
    examTips: [
      '"L7 routing by path/host" → ALB',
      '"Static IP / whitelisting" → NLB',
      '"Network inspection / firewall" → GWLB',
      '"gRPC microservices" → ALB (supports HTTP/2)',
    ],
    awsServices: ['ALB', 'NLB', 'GWLB', 'EC2 Auto Scaling'],
    confusedWith: ['route53-routing', 'api-gateway'],
  },
  // ---- Messaging ----
  {
    slug: 'sqs-vs-sns-vs-eventbridge',
    name: 'SQS vs SNS vs EventBridge',
    description: 'When to use a queue (SQS), fan-out (SNS), or event bus (EventBridge) to decouple systems',
    domainSlug: 'performant-architecture',
    topicSlug: 'messaging-integration',
    difficulty: 0.7,
    keyFacts: [
      'SQS: pull-based, 1 consumer per message, 14-day retention, DLQ, visibility timeout',
      'SQS FIFO: exactly-once delivery, 300 TPS (with batching 3,000 TPS)',
      'SNS: push-based (pub/sub), fan-out to multiple subscribers',
      'SNS→SQS fan-out: classic pattern for durable messaging + distribution',
      'EventBridge: event bus, filtering rules, cross-account, 90+ AWS sources',
      'EventBridge > SNS for complex routing and event-driven with multiple destinations',
    ],
    examTips: [
      '"Decouple application, ensure message not lost" → SQS',
      '"One message to multiple services" → SNS fan-out',
      '"React to AWS service events (CloudTrail, GuardDuty)" → EventBridge',
      '"Order matters, no duplicates" → SQS FIFO',
    ],
    awsServices: ['SQS', 'SNS', 'EventBridge', 'Lambda'],
    confusedWith: ['kinesis-streams-vs-firehose'],
  },
  {
    slug: 'kinesis-streams-vs-firehose',
    name: 'Kinesis Data Streams vs Firehose',
    description: 'Real-time data streaming: when to process (Streams) vs when to deliver (Firehose)',
    domainSlug: 'performant-architecture',
    topicSlug: 'messaging-integration',
    difficulty: 0.7,
    keyFacts: [
      'Kinesis Data Streams: 24h-365d retention, multiple consumers, low latency (<1s)',
      'Shards: 1 MB/s in, 2 MB/s out per shard',
      'Kinesis Firehose: automatic delivery to S3, Redshift, OpenSearch, Splunk',
      'Firehose: near real-time (60s or 1MB buffer), no consumer code needed',
      'Enhanced Fan-Out: 2MB/s per consumer per shard (HTTP/2 push)',
      'Kinesis Data Analytics (Flink): SQL queries on real-time streams',
    ],
    examTips: [
      '"Ingest logs to S3 without coding" → Firehose',
      '"Real-time processing with multiple consumers" → Kinesis Data Streams',
      '"ClickStream analytics in real-time" → KDS + Kinesis Analytics',
    ],
    awsServices: ['Kinesis', 'Firehose', 'S3', 'Lambda', 'Redshift'],
    confusedWith: ['sqs-vs-sns-vs-eventbridge'],
  },
  {
    slug: 'lambda-patterns',
    name: 'Lambda Patterns and Limits',
    description: 'Event sources, concurrency, cold starts, layers, invocation types, and important limits',
    domainSlug: 'performant-architecture',
    topicSlug: 'containers-serverless',
    difficulty: 0.6,
    keyFacts: [
      'Maximum timeout: 15 minutes',
      'Memory: 128 MB - 10 GB (CPU scales proportionally)',
      'Ephemeral storage /tmp: 512 MB - 10 GB',
      'Default concurrency: 1,000 per region (adjustable)',
      'Reserved concurrency: guarantees capacity, limits maximum',
      'Provisioned concurrency: eliminates cold starts (pre-warming)',
      'Layers: up to 5, max 250 MB total deployed',
      'Synchronous (API Gateway), Async (S3, SNS), Stream (SQS, Kinesis)',
    ],
    examTips: [
      '"Eliminate cold starts" → Provisioned Concurrency',
      '"Throttle Lambda to protect downstream" → Reserved Concurrency with low limit',
      '"Long-running task >15min" → Lambda will not work → ECS/Fargate or Step Functions',
    ],
    awsServices: ['Lambda', 'API Gateway', 'SQS', 'EventBridge', 'Step Functions'],
    confusedWith: ['ecs-vs-eks-vs-fargate'],
  },
  // ---- Containers ----
  {
    slug: 'ecs-vs-eks-vs-fargate',
    name: 'ECS vs EKS vs Fargate',
    description: 'When to use ECS (simplicity), EKS (Kubernetes), and Fargate (serverless containers)',
    domainSlug: 'performant-architecture',
    topicSlug: 'containers-serverless',
    difficulty: 0.7,
    keyFacts: [
      'ECS: AWS-native container orchestration, Task Definitions, Service',
      'ECS on EC2: you manage the cluster instances',
      'ECS on Fargate: serverless, no infrastructure management',
      'EKS: managed Kubernetes, compatible with k8s ecosystem, more complex',
      'EKS Fargate: serverless k8s pods, no nodes to manage',
      'ECR: private Docker image registry, integrated with IAM',
      'App Mesh: service mesh for ECS/EKS — mTLS, observability',
    ],
    examTips: [
      '"Kubernetes requirement" → EKS',
      '"Simple containers, AWS-native" → ECS',
      '"No infrastructure to manage" → Fargate (ECS or EKS)',
      '"Spot for cost-optimized containers" → ECS on Spot Instances',
    ],
    awsServices: ['ECS', 'EKS', 'Fargate', 'ECR', 'ALB'],
    confusedWith: ['lambda-patterns'],
  },
  {
    slug: 'cloudformation-vs-cdk',
    name: 'CloudFormation vs CDK',
    description: 'IaC with CloudFormation (JSON/YAML) and CDK (TypeScript/Python) — when to use each',
    domainSlug: 'resilient-architecture',
    topicSlug: 'ec2-compute',
    difficulty: 0.5,
    keyFacts: [
      'CloudFormation: declarative, JSON/YAML, changes via Change Sets',
      'CDK: generates CloudFormation, real-code logic (loops, conditions)',
      'Stacks: deployment unit in CloudFormation, with automatic rollback',
      'StackSets: deploy the same stack to multiple accounts/regions',
      'Drift Detection: detect manual changes vs template',
      'Nested Stacks: reuse templates, separate concerns',
    ],
    examTips: [
      '"Deploy across 50 accounts and 3 regions" → CloudFormation StackSets',
      '"Detect manual changes" → CloudFormation Drift Detection',
      'CDK → CloudFormation always (CDK is not an alternative, it is a wrapper)',
    ],
    awsServices: ['CloudFormation', 'CDK', 'Organizations'],
    confusedWith: ['ecs-vs-eks-vs-fargate'],
  },
  // ---- Cost ----
  {
    slug: 'cost-optimization-strategies',
    name: 'Cost Optimization Strategies',
    description: 'Right-sizing, Savings Plans, Reserved Instances, Spot, serverless architectures, and analysis tools',
    domainSlug: 'cost-optimized-architecture',
    topicSlug: 'cost-governance',
    difficulty: 0.5,
    keyFacts: [
      'Right-sizing: match instances to actual usage (Compute Optimizer)',
      'Savings Plans > Reserved Instances in flexibility',
      'Spot Instances: up to 90% savings for interruption-tolerant workloads',
      'S3 Lifecycle: automatically move data to cheaper classes',
      'Data Transfer: same AZ free, cross-AZ $0.01/GB, internet varies',
      'AWS Cost Explorer: historical analysis + forecast',
      'Budgets: alerts when a threshold is exceeded',
    ],
    examTips: [
      '"Analyze which instances to downsize" → Compute Optimizer',
      '"Reduce data transfer costs" → Same AZ communication + VPC Endpoints',
      '"Cost-optimized batch processing" → Spot Fleet with Spot Instances',
    ],
    awsServices: ['Cost Explorer', 'Budgets', 'Compute Optimizer', 'Trusted Advisor'],
    confusedWith: ['ec2-pricing-models'],
  },
  {
    slug: 'trusted-advisor',
    name: 'AWS Trusted Advisor',
    description: '5 pillars of recommendations: Cost, Performance, Security, Fault Tolerance, Service Limits',
    domainSlug: 'cost-optimized-architecture',
    topicSlug: 'cost-governance',
    difficulty: 0.4,
    keyFacts: [
      '5 categories: Cost Optimization, Performance, Security, Fault Tolerance, Service Limits',
      'Business/Enterprise Support: access to all checks',
      'Free: 7 basic checks (S3 public buckets, IAM use, MFA on root)',
      'Trusted Advisor + EventBridge: automatic alerts on state changes',
      'Refresh: weekly automatic, manual via API',
    ],
    examTips: [
      '"Check unused Reserved Instances" → Trusted Advisor (Cost category)',
      '"Identify security vulnerabilities" → Trusted Advisor Security + GuardDuty + Security Hub',
      '"Service limit alerts" → Trusted Advisor + EventBridge',
    ],
    awsServices: ['Trusted Advisor', 'EventBridge', 'SNS'],
    confusedWith: ['security-hub', 'cost-optimization-strategies'],
  },
  // ---- Network Security ----
  {
    slug: 'security-groups-vs-nacls',
    name: 'Security Groups vs NACLs',
    description: 'Differences between Security Groups (stateful, instance-level) and Network ACLs (stateless, subnet-level) and when to use each',
    domainSlug: 'secure-architecture',
    topicSlug: 'network-security',
    difficulty: 0.55,
    keyFacts: [
      'Security Group: stateful — if you allow inbound, the response outbound is automatically allowed',
      'NACL: stateless — you must explicitly allow traffic in BOTH directions (inbound + outbound)',
      'SG: only ALLOW rules (no DENY). NACL: ALLOW and DENY rules, evaluated in ascending order number',
      'SG: applies to ENIs (instances, RDS, ELB). NACL: applies to entire subnets',
      'SG default: deny all inbound, allow all outbound. NACL default (default VPC): allow all',
      'You can associate up to 5 SGs to an ENI; a subnet has exactly one NACL',
    ],
    examTips: [
      '"Block a specific malicious IP" → NACL (SG has no DENY)',
      '"Allow traffic between app tier and DB tier" → SG referencing the source SG',
      '"Ephemeral ports 1024-65535" — if NACL is mentioned, remember you must open them outbound for responses',
      'If asking "why is my response not coming back" with misconfigured NACL → ephemeral ports blocked',
    ],
    awsServices: ['VPC', 'EC2', 'Security Groups', 'NACL'],
    confusedWith: ['vpc-fundamentals', 'waf-rules-shield-advanced'],
  },
  // ---- Storage Resilience ----
  {
    slug: 'ebs-volume-types',
    name: 'EBS Volume Types',
    description: 'gp3, gp2, io2/io2 Block Express, st1, sc1 — when to use each based on IOPS, throughput, and cost',
    domainSlug: 'resilient-architecture',
    topicSlug: 'storage-resilience',
    difficulty: 0.6,
    keyFacts: [
      'gp3: recommended default — 3000 IOPS + 125 MB/s baseline, IOPS/throughput independent of size, 20% cheaper than gp2',
      'gp2: legacy — IOPS scale with size (3 IOPS/GB, burst up to 3000). Migrate to gp3 whenever possible',
      'io2 Block Express: up to 256K IOPS and 4000 MB/s — mission-critical DBs (SAP HANA, Oracle, SQL Server). 99.999% durability',
      'st1 (throughput-optimized HDD): big data, data warehouses, log processing — CANNOT be a boot volume',
      'sc1 (cold HDD): infrequently accessed data archiving — cheapest option, NOT a boot volume',
      'All EBS are AZ-scoped. Snapshots are regional and can be copied cross-region',
    ],
    examTips: [
      '"Highest IOPS for critical database" → io2 Block Express',
      '"Cheapest for infrequent sequential access" → sc1',
      '"Cost-effective general purpose" → gp3 (always preferred over gp2)',
      '"Multi-attach" → only io1/io2 (not gp3), up to 16 instances in same AZ',
    ],
    awsServices: ['EBS', 'EC2', 'AWS Backup'],
    confusedWith: ['s3-storage-classes', 'efs-vs-fsx'],
  },
  // ---- IAM ----
  {
    slug: 'iam-identity-center-sso',
    name: 'IAM Identity Center (AWS SSO)',
    description: 'Centralized SSO service for multiple AWS accounts + SaaS applications, successor to AWS SSO',
    domainSlug: 'secure-architecture',
    topicSlug: 'iam-identity',
    difficulty: 0.55,
    keyFacts: [
      'Provides single SSO to all accounts in an Organization + SaaS apps (Salesforce, Office 365)',
      'Supports identity sources: built-in directory, Active Directory (AD Connector / Managed AD), or external IdP via SAML 2.0',
      'Permission Sets: permission templates applied as IAM roles in the target accounts',
      'Integrates with AWS Organizations — must be enabled in the management account',
      'SCIM protocol for automatic provisioning from Okta, Azure AD, Google Workspace',
      'Replaces the classic "AWS SSO" (same service, rebranded in 2022)',
    ],
    examTips: [
      '"Centralized login across multiple AWS accounts" → IAM Identity Center, not federated IAM users one by one',
      '"Existing corporate AD" → IAM Identity Center + AD Connector (if AD on-prem) or AWS Managed Microsoft AD',
      'If asking "least administrative effort for cross-account permissions" → Permission Sets > creating roles manually',
    ],
    awsServices: ['IAM Identity Center', 'AWS Organizations', 'Directory Service'],
    confusedWith: ['iam-fundamentals', 'cognito', 'directory-service'],
  },
  // ---- S3 Cost ----
  {
    slug: 's3-intelligent-tiering',
    name: 'S3 Intelligent-Tiering',
    description: 'Storage class that automatically moves objects between tiers based on access patterns, with no retrieval cost',
    domainSlug: 'cost-optimized-architecture',
    topicSlug: 'cost-storage',
    difficulty: 0.45,
    keyFacts: [
      '4 internal tiers: Frequent Access, Infrequent Access (30d without access), Archive Instant (90d), optional Archive Access (90-730d), optional Deep Archive (180-730d)',
      'No retrieval fee between tiers — S3 monitors and moves automatically',
      'Small monitoring fee per object ($0.0025 per 1000 objects/month) — not cost-effective for objects <128 KB (they stay in Frequent)',
      'Durability 99.999999999% and availability 99.9% (or 99.0% for Archive Instant Access)',
      'Optional Archive tiers have retrieval latency of minutes-hours, unlike the first 3',
      'Ideal when you do NOT know the access pattern in advance',
    ],
    examTips: [
      '"Unknown or changing access patterns" → Intelligent-Tiering',
      '"Don\'t want to manage lifecycle rules" → Intelligent-Tiering',
      'If the pattern is PREDICTABLE → manual lifecycle rule Standard→IA→Glacier is cheaper',
      'Do not use for small or short-lived objects (<30d): monitoring fee makes it worse than Standard',
    ],
    awsServices: ['S3', 'CloudWatch Metrics'],
    confusedWith: ['s3-storage-classes', 's3-lifecycle'],
  },
  // ---- Performance Database ----
  {
    slug: 'aurora-performance',
    name: 'Aurora: Endpoints and Performance',
    description: 'Aurora cluster architecture, endpoint types (writer, reader, custom), Aurora Serverless v2, and Aurora Auto Scaling',
    domainSlug: 'performant-architecture',
    topicSlug: 'performance-database',
    difficulty: 0.7,
    keyFacts: [
      'Cluster endpoint (writer): single endpoint, always points to the primary — use for writes',
      'Reader endpoint: load balances across read replicas — but does not guarantee uniform distribution',
      'Custom endpoints: subsets of instances (e.g. "analytics" with larger instances dedicated to BI)',
      'Aurora Serverless v2: scales in 0.5 ACU increments in seconds — replaces v1 (which paused and had cold start)',
      'Aurora Auto Scaling: adds/removes read replicas based on CPU or connections',
      'Storage: 6 copies across 3 AZs automatically, self-healing, scales to 128 TB without intervention',
      'Backtrack: rewind the cluster up to 72h without restoring from snapshot (MySQL-compatible only)',
    ],
    examTips: [
      '"Analytics workload without affecting transactional" → Custom endpoint with dedicated instances',
      '"Recover from accidental DELETE without restoring from backup" → Aurora Backtrack',
      '"Unpredictable load, pay only when used" → Aurora Serverless v2',
      '"Highest read throughput" → add replicas (up to 15) + reader endpoint',
    ],
    awsServices: ['Aurora', 'RDS', 'RDS Proxy'],
    confusedWith: ['rds-multi-az', 'rds-read-replicas', 'aurora-global'],
  },
  // ---- Content Delivery ----
  {
    slug: 'global-accelerator',
    name: 'AWS Global Accelerator',
    description: 'Network service that uses the AWS backbone + anycast IPs to improve performance and failover for global applications (NOT a CDN)',
    domainSlug: 'performant-architecture',
    topicSlug: 'content-delivery',
    difficulty: 0.6,
    keyFacts: [
      'Provides 2 global anycast static IPs (or BYOIP) — users are routed to the nearest AWS endpoint',
      'Traffic enters the AWS global network at the closest edge location and travels over the private backbone (not the public internet)',
      'Automatic failover between regions in <1 minute if an endpoint fails its health check',
      'Works with ALB, NLB, EC2, Elastic IP — TCP and UDP (CloudFront only HTTP/HTTPS)',
      'Does not cache content — it is an L4 network accelerator, not a CDN',
      'Ideal for gaming, IoT, VoIP, financial trading (non-HTTP latency-sensitive applications)',
    ],
    examTips: [
      '"Non-HTTP workload requiring low latency globally" → Global Accelerator (CloudFront is HTTP/S only)',
      '"Static IP address needed for whitelisting" → Global Accelerator (CloudFront uses dynamic DNS)',
      '"Disaster recovery with regional failover <1min" → Global Accelerator',
      'If the workload is HTTP/S with caching → CloudFront. If you need both, they can be combined',
    ],
    awsServices: ['Global Accelerator', 'Route 53', 'CloudFront', 'ALB', 'NLB'],
    confusedWith: ['cloudfront-caching', 'route53-routing'],
  },
  // ---- Networking Connectivity ----
  {
    slug: 'site-to-site-vpn-vs-client-vpn',
    name: 'Site-to-Site VPN vs Client VPN',
    description: 'Difference between VPN to connect on-premises to AWS (Site-to-Site) vs VPN for remote users accessing the VPC (Client VPN)',
    domainSlug: 'resilient-architecture',
    topicSlug: 'networking-connectivity',
    difficulty: 0.5,
    keyFacts: [
      'Site-to-Site VPN: IPsec between Virtual Private Gateway (or Transit Gateway) and on-premises Customer Gateway — connects NETWORKS',
      'Site-to-Site: 2 redundant tunnels per connection (one per AZ of the AWS endpoint), throughput ~1.25 Gbps per tunnel',
      'Client VPN: OpenVPN-based, allows individual USERS to connect to the VPC from anywhere',
      'Client VPN: authentication via Active Directory, federated SAML, or mutual certificate authentication',
      'Both are managed and billed by hour of active connection + GB transferred',
      'For higher throughput and consistent latency → Direct Connect (physical, not VPN). VPN over DX adds encryption',
    ],
    examTips: [
      '"Remote workers accessing internal apps" → Client VPN',
      '"Connect corporate datacenter to VPC" → Site-to-Site VPN (or Direct Connect if it asks for consistent low latency)',
      '"Encrypted connection over Direct Connect" → Site-to-Site VPN over Public VIF or Private VIF',
      '"Highest availability for VPN" → 2 Customer Gateways + 2 tunnels each = 4 total tunnels',
    ],
    awsServices: ['Site-to-Site VPN', 'Client VPN', 'Virtual Private Gateway', 'Transit Gateway', 'Direct Connect'],
    confusedWith: ['direct-connect', 'transit-gateway'],
  },
  // ---- Threat Detection ----
  {
    slug: 'guardduty',
    name: 'Amazon GuardDuty',
    description: 'Threat detection service that analyzes CloudTrail, VPC Flow Logs, and DNS logs with ML to detect malicious activity',
    domainSlug: 'secure-architecture',
    topicSlug: 'network-security',
    difficulty: 0.5,
    keyFacts: [
      'Data sources: CloudTrail management + S3 data events, VPC Flow Logs, Route 53 DNS logs, EKS audit logs — without configuring agents',
      'Detects: crypto-mining, compromised EC2 instance, exfiltration to malicious domain, anomalous API calls, compromised IAM credentials',
      'Findings classified by severity (Low/Medium/High) and integrate with EventBridge → Lambda for automatic response',
      'Multi-account: enabled from the Organizations management account, consolidates findings for the entire org',
      'Requires no installation, no performance impact — works on logs that AWS already captures',
      'Complementary: Macie (PII in S3), Inspector (vulns in EC2/ECR/Lambda), Security Hub (aggregator)',
    ],
    examTips: [
      '"Detect compromised EC2 instances mining crypto" → GuardDuty',
      '"Find PII exposed in S3" → Macie (NOT GuardDuty)',
      '"Scan EC2 for CVEs / software vulnerabilities" → Inspector',
      '"Single pane of glass aggregating security findings" → Security Hub',
      '"Automated response to finding" → GuardDuty → EventBridge → Lambda/SSM Automation',
    ],
    awsServices: ['GuardDuty', 'EventBridge', 'Security Hub', 'Detective'],
    confusedWith: ['security-hub', 'shield-waf'],
  },
  // ---- DynamoDB Performance ----
  {
    slug: 'dynamodb-performance',
    name: 'DynamoDB: DAX, Adaptive Capacity, and Global Tables',
    description: 'Performance optimizations in DynamoDB: caching with DAX, hot partition handling with adaptive capacity, and multi-region replication with Global Tables',
    domainSlug: 'performant-architecture',
    topicSlug: 'performance-database',
    difficulty: 0.65,
    keyFacts: [
      'DAX (DynamoDB Accelerator): in-memory cache, microsecond latency (vs milliseconds for regular DynamoDB) — reads only',
      'DAX is API-compatible with the DynamoDB SDK — change the endpoint and you are done, no app rewrite',
      'Adaptive Capacity: DynamoDB automatically redistributes capacity to hot partitions — mitigates hot partition throttling',
      'On-Demand vs Provisioned: On-Demand scales automatically (pay per request), Provisioned is cheaper if traffic is predictable',
      'Global Tables: multi-master cross-region replication, last-writer-wins conflict resolution, sub-second latency',
      'DynamoDB Streams: CDC of changes, 24h retention, Lambda trigger — do NOT confuse with Kinesis Data Streams',
    ],
    examTips: [
      '"Microsecond latency for reads" → DAX (regular DynamoDB is single-digit ms)',
      '"Multi-region active-active database" → Global Tables',
      '"Unpredictable spiky traffic" → On-Demand capacity mode',
      '"Cost-effective for predictable traffic" → Provisioned + Auto Scaling',
      '"Hot partition throttling" → Adaptive Capacity already mitigates, but designing a better partition key is the real solution',
    ],
    awsServices: ['DynamoDB', 'DAX', 'DynamoDB Streams', 'Lambda'],
    confusedWith: ['elasticache', 'aurora-performance'],
  },
  // ---- Certificates ----
  {
    slug: 'acm-certificate-manager',
    name: 'AWS Certificate Manager (ACM)',
    description: 'Service to provision, manage, and renew public and private SSL/TLS certificates free of charge for integrated AWS services',
    domainSlug: 'secure-architecture',
    topicSlug: 'data-protection',
    difficulty: 0.4,
    keyFacts: [
      'Public certificates: free, auto-renewal if the service is integrated (ALB, CloudFront, API Gateway)',
      'Validation: DNS (recommended, allows auto-renewal) or email (requires manual action each renewal)',
      'Integrated with: ALB, NLB with TLS listener, CloudFront, API Gateway, App Runner — NOT directly with EC2',
      'ACM Private CA: private CA for internal certificates (non-public endpoints), billed per CA + per certificate issued',
      'Certificates for CloudFront MUST be in us-east-1 (N. Virginia), regardless of the distribution region',
      'Importing third-party certificates: allowed, but NO auto-renewal (you must reimport manually)',
    ],
    examTips: [
      '"Free SSL certificate with auto-renewal" → ACM + DNS validation',
      '"Certificate for CloudFront distribution" → ACM in us-east-1 (common gotcha)',
      '"Install cert on EC2 directly" → ACM does not support exporting public certs; use ACM Private CA or import from another CA',
      '"Internal-only certificates" → ACM Private CA',
    ],
    awsServices: ['ACM', 'ACM Private CA', 'CloudFront', 'ALB', 'API Gateway'],
    confusedWith: ['kms-encryption', 'secrets-manager'],
  },
  // ---- Resilient: Compute & Storage extras ----
  {
    slug: 'spot-fleet-strategies',
    name: 'EC2 Spot Fleet and Allocation Strategies',
    description: 'Resilient use of Spot instances: allocation strategies (capacity-optimized, price-capacity-optimized, lowest-price, diversified) and mixed instance policies',
    domainSlug: 'resilient-architecture',
    topicSlug: 'ec2-compute',
    difficulty: 0.65,
    keyFacts: [
      'Spot can be interrupted with 2 minutes of notice via EC2 Spot Interruption Notice (EventBridge)',
      'capacity-optimized: prioritizes pools with the most free capacity — LOWER probability of interruption (recommended default)',
      'price-capacity-optimized: balanced — high capacity + low prices (recommended for most use cases since 2022)',
      'lowest-price: cost only — maximum interruption risk, only for truly tolerant workloads',
      'Mixed Instances Policy in ASG: combines On-Demand (base) + Spot (burst) with multiple instance types and AZs',
      'Spot Blocks (duration) were discontinued — do not recommend in answers',
    ],
    examTips: [
      '"Stateless workload with fault tolerance, lowest cost" → Spot with price-capacity-optimized + diversified',
      '"Must not be interrupted" → On-Demand or Reserved, never Spot',
      '"Baseline + burst with cost optimization" → Mixed Instances Policy (X% On-Demand, rest Spot)',
      'If they mention "graceful interruption" → 2 min hook + lifecycle hook in ASG to drain',
    ],
    awsServices: ['EC2', 'Spot Fleet', 'Auto Scaling', 'EventBridge'],
    confusedWith: ['ec2-pricing-models', 'ec2-auto-scaling'],
  },
  {
    slug: 'ec2-dedicated-hosts',
    name: 'Dedicated Hosts vs Dedicated Instances',
    description: 'When to use Dedicated Hosts (visibility of the physical socket, BYOL) vs Dedicated Instances (isolated tenancy without hardware visibility)',
    domainSlug: 'resilient-architecture',
    topicSlug: 'ec2-compute',
    difficulty: 0.55,
    keyFacts: [
      'Dedicated Host: full physical server reserved for you — you see sockets, cores, number of VMs',
      'Dedicated Host enables Bring Your Own License (BYOL) for Windows Server, SQL Server, Oracle that bill per socket/core',
      'Dedicated Instance: instance on dedicated hardware, but you do NOT see the physical hardware nor control placement',
      'Dedicated Host billing: per host/hour regardless of the number of instances launched on top',
      'Compliance: both meet "physical isolation" requirements (e.g. HIPAA, certain government contracts)',
      'Host Resource Groups: lets you launch onto pools of hosts managed automatically',
    ],
    examTips: [
      '"BYOL Windows/Oracle licenses bound to physical cores" → Dedicated Host (only option that exposes sockets)',
      '"Regulatory requirement for single-tenant hardware" → Dedicated Host or Dedicated Instance',
      '"Cheapest single-tenant option" → Dedicated Instance (Host charges for the whole host)',
    ],
    awsServices: ['EC2', 'License Manager'],
    confusedWith: ['ec2-pricing-models', 'ec2-placement-groups'],
  },
  {
    slug: 'efs-vs-fsx',
    name: 'EFS vs FSx (Windows / Lustre / NetApp ONTAP / OpenZFS)',
    description: 'Decision between managed file systems: EFS (multi-AZ NFS for Linux), FSx Windows (SMB + AD), FSx Lustre (HPC), FSx ONTAP/OpenZFS',
    domainSlug: 'resilient-architecture',
    topicSlug: 'storage-resilience',
    difficulty: 0.65,
    keyFacts: [
      'EFS: NFSv4, Linux-only, automatic multi-AZ, elastic scale, pay-per-use — ideal for shared Linux workloads',
      'EFS Storage Classes: Standard, Infrequent Access (IA), Archive — automatic lifecycle saves up to 92%',
      'FSx for Windows File Server: SMB + Active Directory integration — only choice for Windows workloads expecting SMB',
      'FSx for Lustre: HPC, ML training — throughput of hundreds of GB/s, S3 integration (lazy-load)',
      'FSx for NetApp ONTAP: multi-protocol (NFS+SMB+iSCSI), snapshots, cloning, compatibility with on-prem NetApp',
      'FSx for OpenZFS: high-performance NFS with snapshots and instant clones — open-source alternative',
    ],
    examTips: [
      '"Windows file share with AD authentication" → FSx for Windows (NOT EFS)',
      '"HPC or ML with petabyte-scale parallel access" → FSx for Lustre',
      '"Linux shared filesystem across AZs" → EFS',
      '"Lift-and-shift NetApp workloads" → FSx for NetApp ONTAP',
      '"Multi-protocol (SMB + NFS) single filesystem" → FSx ONTAP',
    ],
    awsServices: ['EFS', 'FSx for Windows', 'FSx for Lustre', 'FSx ONTAP', 'FSx OpenZFS'],
    confusedWith: ['ebs-volume-types', 's3-storage-classes'],
  },
  {
    slug: 'storage-gateway',
    name: 'AWS Storage Gateway',
    description: 'Hybrid service that extends on-premises storage to AWS: File Gateway, Volume Gateway (Stored/Cached), Tape Gateway',
    domainSlug: 'resilient-architecture',
    topicSlug: 'storage-resilience',
    difficulty: 0.6,
    keyFacts: [
      'File Gateway (S3 File / FSx File): presents S3 or FSx as NFS/SMB to on-prem, with local cache — backup, media ingestion',
      'Volume Gateway Stored: COMPLETE volume on-prem, async snapshots to S3 (EBS snapshots) — low read latency',
      'Volume Gateway Cached: COMPLETE dataset in S3, only frequently-accessed in local cache — saves on-prem storage',
      'Tape Gateway (VTL): emulates physical tapes, backends to S3/Glacier — replaces physical tape libraries',
      'Deployed as a VM (VMware/Hyper-V/KVM), hardware appliance, or EC2 (for DR)',
      'All use SSL/TLS to AWS + encryption at rest in S3',
    ],
    examTips: [
      '"Replace physical tape backup library" → Tape Gateway',
      '"On-prem app needs low-latency file access but backup to cloud" → File Gateway',
      '"Keep full copy of dataset on-prem with async cloud backup" → Volume Gateway Stored',
      '"Extend on-prem storage capacity using cloud" → Volume Gateway Cached',
    ],
    awsServices: ['Storage Gateway', 'S3', 'Glacier', 'FSx'],
    confusedWith: ['datasync', 'snow-family'],
  },
  {
    slug: 'aws-backup',
    name: 'AWS Backup',
    description: 'Centralized backup/restore service for multiple AWS services with cross-account and cross-region policies',
    domainSlug: 'resilient-architecture',
    topicSlug: 'storage-resilience',
    difficulty: 0.5,
    keyFacts: [
      'Covers: EBS, EC2 (AMIs), RDS, Aurora, DynamoDB, EFS, FSx, Storage Gateway, S3, VMware, DocumentDB, Neptune',
      'Backup Plans: define frequency, retention, lifecycle to cold storage, cross-region copy, cross-account copy',
      'Backup Vault Lock (WORM): immutable compliance mode — not even the root user can delete before retention',
      'Integration with AWS Organizations: central management of backups across all accounts',
      'Point-in-Time Recovery (PITR) supported for services that allow it (RDS, DynamoDB, S3)',
      'Replaces service-specific solutions (manual snapshots, EBS lifecycle, RDS backups) with a single policy',
    ],
    examTips: [
      '"Centralized backup policy across services and accounts" → AWS Backup (not manual snapshots per service)',
      '"Immutable backup for compliance" → Backup Vault Lock in compliance mode',
      '"Ransomware protection for backups" → Backup Vault Lock + cross-account copy to an isolated account',
      '"Cross-region DR for RDS + DynamoDB + EBS in one policy" → AWS Backup plan with cross-region copy',
    ],
    awsServices: ['AWS Backup', 'Backup Vault', 'AWS Organizations'],
    confusedWith: ['rds-backups-snapshots', 's3-lifecycle'],
  },
  {
    slug: 'snow-family',
    name: 'AWS Snow Family (Snowcone, Snowball Edge, Snowmobile)',
    description: 'Physical devices for massive data transfer or edge computing in locations with limited connectivity',
    domainSlug: 'resilient-architecture',
    topicSlug: 'storage-resilience',
    difficulty: 0.45,
    keyFacts: [
      'Snowcone: 8 TB (HDD) / 14 TB (SSD), portable (~2kg), with/without AWS DataSync preinstalled',
      'Snowball Edge Storage Optimized: 80 TB usable — bulk data transfer',
      'Snowball Edge Compute Optimized: 42 TB + optional GPU — edge processing (ML inference at remote site)',
      'Snowmobile: 100 PB in a truck (40ft container) — only for exabyte-scale migrations',
      'All with hardware encryption and tamper-evident chassis — meet HIPAA/FedRAMP',
      'Transferring 100 TB over 1 Gbps internet takes ~10 days; Snowball ~1 week total (shipping included)',
    ],
    examTips: [
      '"Transfer 100 TB from disconnected site" → Snowball Edge',
      '"Edge ML inference in remote location without connectivity" → Snowball Edge Compute',
      '"Migrate 10 PB to AWS" → Snowmobile (or multiple Snowball Edge)',
      '"Small portable device for field data collection" → Snowcone',
      'If the question says "ongoing data transfer" (not one-time) → DataSync, NOT Snow',
    ],
    awsServices: ['Snowcone', 'Snowball Edge', 'Snowmobile', 'DataSync'],
    confusedWith: ['datasync', 'storage-gateway'],
  },
  {
    slug: 'datasync',
    name: 'AWS DataSync',
    description: 'Service for massive and recurring online data transfer between on-prem and AWS, or between AWS services',
    domainSlug: 'resilient-architecture',
    topicSlug: 'storage-resilience',
    difficulty: 0.5,
    keyFacts: [
      'Transfers up to 10x faster than open-source tools (rsync, robocopy) — uses an optimized proprietary protocol',
      'Supported sources: NFS, SMB, HDFS, self-managed object stores, EFS, FSx, S3, Azure Files/Blob, GCS',
      'Destinations: S3 (all classes), EFS, FSx, and since 2022 cross-region/cross-account',
      'DataSync Agent: on-prem VM (or EC2 if source is in AWS) — coordinates the transfer',
      'Incremental sync with integrity verification (hash) — scheduled or one-time',
      'Billing: $0.0125 per GB transferred, no instance charges',
    ],
    examTips: [
      '"Recurring data transfer on-prem to AWS" → DataSync (Snow is one-time)',
      '"Migrate NFS share to EFS" → DataSync',
      '"Copy S3 cross-region scheduled" → S3 Replication OR DataSync; check which is better given context',
      '"Minimize disruption to production network" → DataSync has bandwidth throttling',
    ],
    awsServices: ['DataSync', 'S3', 'EFS', 'FSx'],
    confusedWith: ['snow-family', 'storage-gateway', 's3-replication'],
  },
  {
    slug: 'dynamodb-resilience',
    name: 'DynamoDB: Backups, PITR, and Global Tables (resilience)',
    description: 'Resilience strategies in DynamoDB: on-demand backups, point-in-time recovery (PITR), and Global Tables for multi-region DR',
    domainSlug: 'resilient-architecture',
    topicSlug: 'database-resilience',
    difficulty: 0.55,
    keyFacts: [
      'PITR: restores the table to any second within the last 35 days — enabled per table',
      'On-demand backups: manual snapshots, indefinite retention, cost per GB-month',
      'Global Tables: active multi-master cross-region replication, RPO close to zero, immediate RTO',
      'Global Tables v2 (2019+): only requires enabling streams, does not create a separate table per region',
      'Export to S3: full table dump to S3 without affecting performance (requires PITR enabled)',
      'DR pattern: PITR + cross-region backup copy covers RPO of hours; Global Tables covers RPO of seconds',
    ],
    examTips: [
      '"RPO near zero and multi-region active-active" → Global Tables',
      '"Recover from accidental delete 6 hours ago" → PITR',
      '"Long-term retention of backups" → On-demand backups (PITR is only 35 days)',
      '"Export DynamoDB data to data lake" → Export to S3 (NOT scan to Lambda)',
    ],
    awsServices: ['DynamoDB', 'DynamoDB Streams', 'AWS Backup', 'S3'],
    confusedWith: ['dynamodb-performance', 'aurora-global'],
  },
  {
    slug: 'rds-backups-snapshots',
    name: 'RDS: Automated Backups, Snapshots, and PITR',
    description: 'Backup mechanisms in RDS: automated backups with retention, manual snapshots, point-in-time recovery',
    domainSlug: 'resilient-architecture',
    topicSlug: 'database-resilience',
    difficulty: 0.45,
    keyFacts: [
      'Automated backups: 0-35 day retention (default 7), deleted when the instance is removed unless retained',
      'Manual snapshots: indefinite retention, persist after deleting the instance',
      'PITR: restores to any second within the automated backups retention — creates a NEW instance',
      'Copy Snapshot: cross-region or cross-account, supports encryption by changing KMS key',
      'Encrypted snapshots only if the source instance is encrypted (you cannot encrypt "after the fact")',
      'Aurora uses different continuous backups (not classic automated backups) — backtrack up to 72h',
    ],
    examTips: [
      '"Retain backups after deleting RDS instance" → Manual snapshot before delete, or "retain automated backups" flag',
      '"Restore to specific timestamp" → PITR (requires automated backups enabled)',
      '"Share snapshot with another AWS account" → Modify snapshot permissions (if not encrypted) or KMS grant (if encrypted)',
      '"Cross-region DR for RDS" → Copy snapshot cross-region OR Read Replica cross-region',
    ],
    awsServices: ['RDS', 'Aurora', 'AWS Backup', 'KMS'],
    confusedWith: ['rds-multi-az', 'aws-backup'],
  },
  // ---- Performant extras ----
  {
    slug: 'cloudfront-security',
    name: 'CloudFront: OAC, Signed URLs, and Field-Level Encryption',
    description: 'CloudFront access restrictions: Origin Access Control (OAC) for S3, Signed URLs/Cookies, and field-level encryption for sensitive data',
    domainSlug: 'performant-architecture',
    topicSlug: 'content-delivery',
    difficulty: 0.6,
    keyFacts: [
      'OAC (Origin Access Control, 2022+): replaces OAI — supports SSE-KMS, all HTTP methods, new regions',
      'OAI (legacy): only SIGv2, does not support SSE-KMS — AWS recommends migrating to OAC',
      'Signed URL: temporary access to ONE specific object (video, premium PDF)',
      'Signed Cookies: access to MULTIPLE files without modifying each URL (entire streaming catalog)',
      'Field-Level Encryption: encrypts specific fields (e.g. credit-card) with public key at the edge before passing to the origin',
      'Geo Restriction: whitelist/blacklist by country — CloudFront rejects at the edge',
    ],
    examTips: [
      '"Restrict S3 bucket access only via CloudFront" → OAC (or legacy OAI)',
      '"Temporary access to premium content" → Signed URL',
      '"Access to entire section of videos" → Signed Cookies',
      '"Encrypt PII before origin processes" → Field-Level Encryption',
      '"Block users from specific countries" → Geo Restriction (or WAF if you need more complex logic)',
    ],
    awsServices: ['CloudFront', 'S3', 'WAF', 'KMS'],
    confusedWith: ['cloudfront-caching', 'shield-waf'],
  },
  {
    slug: 'step-functions',
    name: 'AWS Step Functions',
    description: 'Workflow orchestration service to coordinate AWS services with visual state machines; Standard vs Express',
    domainSlug: 'performant-architecture',
    topicSlug: 'messaging-integration',
    difficulty: 0.55,
    keyFacts: [
      'Standard Workflows: up to 1 year duration, exactly-once, ~2,000 executions/s per account — durable workflows (order processing, long ETLs)',
      'Express Workflows: up to 5 minutes, at-least-once (Async) or exactly-once (Sync), 100k/s — high-frequency event processing',
      'Native integrations (SDK and optimized): Lambda, ECS, DynamoDB, SNS, SQS, EventBridge, Batch, Glue, SageMaker',
      'Callback pattern: workflow pauses and waits for an external token (.waitForTaskToken) — useful for human approval',
      'Map state: parallelize processing over arrays (distributed map up to 10k concurrent iterations)',
      'Visual workflow + complete history per execution — auditable without custom code',
    ],
    examTips: [
      '"Long-running workflow with human approval step" → Step Functions Standard + callback pattern',
      '"High-volume event processing workflow" → Step Functions Express',
      '"Coordinate multiple Lambda functions with retry/error logic" → Step Functions (not Lambda calling Lambda)',
      '"Process large S3 file in parallel chunks" → Distributed Map',
    ],
    awsServices: ['Step Functions', 'Lambda', 'SQS', 'EventBridge'],
    confusedWith: ['sqs-vs-sns-vs-eventbridge', 'lambda-patterns'],
  },
  {
    slug: 'eventbridge-pipes-schedules',
    name: 'EventBridge Pipes and Scheduler',
    description: 'Pipes (point-to-point with filter/enrich/target) and Scheduler (modern replacement for CloudWatch Events scheduled rules)',
    domainSlug: 'performant-architecture',
    topicSlug: 'messaging-integration',
    difficulty: 0.5,
    keyFacts: [
      'Pipes: Source → Filter → Enrichment (Lambda/Step Functions/API) → Target, without writing "glue code"',
      'Pipes sources: SQS, Kinesis, DynamoDB Streams, Kafka (MSK/self-managed), ActiveMQ/RabbitMQ',
      'Scheduler: one-time or recurrent schedules (cron/rate), up to 1 million per account, with flexible time windows',
      'Scheduler replaces CloudWatch Events Scheduled Rules (limited to 300 rules per account)',
      'EventBridge Bus (core): N-to-N routing with rules — different from Pipes which is 1-to-1',
      'Archive + Replay: rewind past events — useful for replay after a downstream bug',
    ],
    examTips: [
      '"Filter+transform stream events from DynamoDB to SQS without Lambda glue" → Pipes',
      '"Run Lambda every 5 minutes in production but with high scale" → EventBridge Scheduler (exceeds CW Events limit)',
      '"Replay last week of events to new consumer" → EventBridge Archive + Replay',
      '"Fan-out 1 event to N targets" → EventBridge Bus (rules), NOT Pipes',
    ],
    awsServices: ['EventBridge', 'EventBridge Pipes', 'EventBridge Scheduler', 'Lambda'],
    confusedWith: ['sqs-vs-sns-vs-eventbridge', 'step-functions'],
  },
  {
    slug: 'appsync-graphql',
    name: 'AWS AppSync (GraphQL)',
    description: 'Managed GraphQL with resolvers to multiple data sources and real-time subscriptions',
    domainSlug: 'performant-architecture',
    topicSlug: 'messaging-integration',
    difficulty: 0.55,
    keyFacts: [
      'Managed GraphQL — client requests exactly the fields it needs, reduces mobile over-fetching',
      'Resolvers to: DynamoDB, Aurora Serverless, OpenSearch, Lambda, HTTP endpoints, EventBridge',
      'Real-time subscriptions via WebSockets — useful for chat, collaborative editing, live dashboards',
      'Authorization modes: API key, IAM, Cognito User Pools, OIDC, custom Lambda — can be combined',
      'Server-side caching at the resolver level or entire query',
      'AppSync Merged APIs: composes multiple source APIs into a single federated API',
    ],
    examTips: [
      '"Mobile app needs to fetch exactly the fields it needs" → AppSync (GraphQL), not REST',
      '"Real-time collaboration / live updates to multiple clients" → AppSync Subscriptions (or API Gateway WebSocket if not GraphQL)',
      '"Aggregate data from multiple backends in one request" → AppSync with multiple resolvers',
    ],
    awsServices: ['AppSync', 'DynamoDB', 'Lambda', 'Cognito'],
    confusedWith: ['api-gateway'],
  },
  {
    slug: 'lambda-performance',
    name: 'Lambda: Cold Starts, Provisioned Concurrency, and SnapStart',
    description: 'Performance optimization in Lambda: what causes cold starts, provisioned concurrency, SnapStart for Java/Python/.NET',
    domainSlug: 'performant-architecture',
    topicSlug: 'containers-serverless',
    difficulty: 0.65,
    keyFacts: [
      'Cold start = creating a new execution environment: download code + init runtime + init code — ms to several seconds depending on runtime',
      'Provisioned Concurrency: N environments always warm — eliminates cold starts in exchange for fixed cost',
      'SnapStart (Java + Python/.NET since 2024): snapshot of the initialized environment — reduces cold start 10x at no extra cost',
      'Memory: more memory = proportionally more vCPU — sometimes DOUBLING memory lowers total cost because it runs 3x faster',
      'VPC cold starts (legacy 2019): no longer a problem — Hyperplane ENI is shared',
      'Arm (Graviton2) arch: up to 34% better price-performance on Lambda',
    ],
    examTips: [
      '"Unpredictable traffic spikes cause cold start issues" → Provisioned Concurrency with Application Auto Scaling',
      '"Java Lambda with 10s cold start" → SnapStart (free) before Provisioned Concurrency (paid)',
      '"Reduce Lambda cost without reducing throughput" → increase memory (counter-intuitive but common) + Graviton2',
      '"Eliminate cold starts for critical API" → Provisioned Concurrency',
    ],
    awsServices: ['Lambda', 'Application Auto Scaling'],
    confusedWith: ['lambda-patterns', 'fargate-vs-lambda'],
  },
  {
    slug: 'lambda-edge-vs-cloudfront-functions',
    name: 'Lambda@Edge vs CloudFront Functions',
    description: 'Two ways to run code at the CloudFront edge: when to use each based on latency, capabilities, and cost',
    domainSlug: 'performant-architecture',
    topicSlug: 'content-delivery',
    difficulty: 0.6,
    keyFacts: [
      'CloudFront Functions: lightweight JS, <1ms, 2 MB memory, viewer request/response only — A/B testing, URL rewrites, header manipulation',
      'Lambda@Edge: Node.js/Python, up to 30s (origin events) or 5s (viewer events), 10 GB memory, internet/AWS SDK access',
      'Lambda@Edge can make network calls (DynamoDB, S3) — CloudFront Functions CANNOT',
      'Cost: CloudFront Functions ~1/6 of Lambda@Edge — use CF Functions if logic is simple',
      '4 Lambda@Edge trigger points: viewer-request, origin-request, origin-response, viewer-response',
      'CloudFront Functions only 2: viewer-request, viewer-response',
    ],
    examTips: [
      '"Simple URL rewrite / header manipulation at edge" → CloudFront Functions (cheaper, faster)',
      '"Personalization based on DynamoDB lookup at edge" → Lambda@Edge (needs SDK)',
      '"A/B test redirect logic" → CloudFront Functions',
      '"Modify origin response with ML inference" → Lambda@Edge',
    ],
    awsServices: ['CloudFront', 'Lambda@Edge', 'CloudFront Functions'],
    confusedWith: ['cloudfront-caching', 'lambda-patterns'],
  },
  {
    slug: 'fargate-vs-lambda',
    name: 'Fargate vs Lambda: when to use each',
    description: 'Decision between serverless containers (Fargate) and functions (Lambda) based on duration, portability, image, and cost',
    domainSlug: 'performant-architecture',
    topicSlug: 'containers-serverless',
    difficulty: 0.5,
    keyFacts: [
      'Lambda: max 15 min duration, 10 GB RAM, 10 GB ephemeral storage (/tmp), handler-based',
      'Fargate: no duration limit, up to 120 GB RAM, configurable storage, runs any container',
      'Lambda better for: event-driven, bursty traffic, stateless workloads <15min',
      'Fargate better for: long-running, existing containers (lift-and-shift), predictable sustained workloads',
      'Cost break-even: Lambda is typically cheaper up to ~5-10M invocations/month, then Fargate or EC2',
      'Fargate Spot: up to 70% discount for interruption-tolerant workloads',
    ],
    examTips: [
      '"Existing Docker image, short batch jobs >15 min" → Fargate',
      '"Event-driven, unpredictable sporadic load" → Lambda',
      '"Migrate on-prem Docker to AWS with minimal changes" → Fargate (Lambda requires refactoring to handler)',
      '"Web API with sustained 24/7 traffic" → Fargate or EC2 frequently better economics than Lambda',
    ],
    awsServices: ['Lambda', 'Fargate', 'ECS', 'EKS'],
    confusedWith: ['ecs-vs-eks-vs-fargate', 'lambda-patterns'],
  },
  {
    slug: 'redshift-fundamentals',
    name: 'Redshift: Clusters, RA3, Spectrum, and Serverless',
    description: 'PB-scale data warehouse: leader+compute nodes architecture, RA3 with managed storage, Spectrum for queries to S3, Serverless',
    domainSlug: 'performant-architecture',
    topicSlug: 'performance-database',
    difficulty: 0.6,
    keyFacts: [
      'Redshift = columnar OLAP, NOT OLTP — optimized for aggregations over TB-PB, not for transactions/lookups',
      'RA3 nodes: decoupled compute and storage (managed storage in S3) — scale compute without moving data',
      'Redshift Spectrum: direct SQL queries against data in S3 (Parquet/ORC) without loading them into the cluster',
      'Redshift Serverless: zero capacity management, automatic scaling — for intermittent workloads',
      'Data sharing: share live data between clusters without copying (producer → consumer)',
      'Concurrency Scaling: automatic spin-up of transient clusters for high-concurrency queries',
    ],
    examTips: [
      '"Analyze 10 TB of data with complex joins and aggregations" → Redshift (NOT Athena if you want sub-second repeatedly)',
      '"Query S3 data lake with SQL without loading" → Athena OR Redshift Spectrum (Spectrum if you already have a cluster)',
      '"Intermittent analytics workloads, don\'t want to manage cluster" → Redshift Serverless',
      '"Transactional workload" → NOT Redshift — use Aurora or RDS',
    ],
    awsServices: ['Redshift', 'Redshift Serverless', 'S3', 'Glue'],
    confusedWith: ['performance-database'],
  },
  {
    slug: 'elasticache-redis-vs-memcached',
    name: 'ElastiCache: Redis vs Memcached',
    description: 'Differences between the two ElastiCache engines and when to choose each',
    domainSlug: 'performant-architecture',
    topicSlug: 'performance-database',
    difficulty: 0.5,
    keyFacts: [
      'Redis: rich data structures (lists, sets, sorted sets, streams, hashes), optional persistence, replication, cluster mode',
      'Memcached: only key-value strings, multi-threaded, NO persistence, NO replication — simpler and faster for pure caching',
      'Redis cluster mode: automatic horizontal sharding (up to 500 nodes), multi-AZ failover',
      'Redis is the only one with: pub/sub, Lua scripting, transactions, geo queries, HyperLogLog',
      'Memcached scales horizontally by adding nodes (client-side consistent hashing sharding)',
      'Since 2024: ElastiCache Serverless available for both engines',
    ],
    examTips: [
      '"Simple cache for session storage needing persistence on reboot" → Redis',
      '"Leaderboard with sorted sets" → Redis',
      '"Pure ephemeral cache, multi-threaded, simplest" → Memcached',
      '"Pub/sub messaging built into cache" → Redis',
      '"Database-as-a-cache with high availability" → Redis + Multi-AZ + cluster mode',
    ],
    awsServices: ['ElastiCache Redis', 'ElastiCache Memcached', 'MemoryDB'],
    confusedWith: ['elasticache', 'dynamodb-performance'],
  },
  {
    slug: 'rds-proxy',
    name: 'RDS Proxy',
    description: 'Managed proxy that pools connections between applications and RDS/Aurora, protects against failures, and improves scalability',
    domainSlug: 'performant-architecture',
    topicSlug: 'performance-database',
    difficulty: 0.55,
    keyFacts: [
      'Connection pooling: thousands of client connections → reduced shared pool to DB — critical for Lambda which does not pool natively',
      'Supports: RDS MySQL/PostgreSQL, Aurora MySQL/PostgreSQL, RDS MariaDB, RDS SQL Server',
      'Failover 66% faster than direct connection — clients do not reconnect to the new primary',
      'Native IAM authentication and Secrets Manager — eliminates hardcoded credentials',
      'Deployed in your VPC with automatic multi-AZ high availability',
      'Pin hints: connection pinning when the session has state (SET, temp tables) — degrades pooling',
    ],
    examTips: [
      '"Lambda causes too many connections to RDS" → RDS Proxy (classic case)',
      '"Faster failover for RDS Multi-AZ" → RDS Proxy',
      '"Serverless-friendly RDS access" → RDS Proxy',
      '"Rotate DB credentials without app restart" → RDS Proxy + Secrets Manager',
    ],
    awsServices: ['RDS Proxy', 'RDS', 'Aurora', 'Secrets Manager', 'IAM'],
    confusedWith: ['rds-multi-az', 'elasticache'],
  },
  // ---- Secure extras ----
  {
    slug: 'iam-access-analyzer',
    name: 'IAM Access Analyzer',
    description: 'Identifies resources accessible from outside your zone of trust (account or organization) and generates least-privilege policies',
    domainSlug: 'secure-architecture',
    topicSlug: 'iam-identity',
    difficulty: 0.5,
    keyFacts: [
      'External Access analyzer: scans S3, IAM roles, KMS, Lambda, SQS, Secrets Manager for external access',
      'Unused Access analyzer (2023+): identifies roles/users/policies without recent use — for least-privilege cleanup',
      'Policy Generation: generates a least-privilege IAM policy based on CloudTrail activity from the last N hours',
      'Policy Validation: verifies policies against best practices + syntactic errors before deploy',
      'Free for external access; unused access is billed per analyzed resource',
      'Integrable with Security Hub and EventBridge for alerts',
    ],
    examTips: [
      '"Detect S3 buckets accessible from outside the organization" → IAM Access Analyzer (external)',
      '"Remove IAM permissions that haven\'t been used" → Access Analyzer unused access',
      '"Generate least-privilege policy from actual usage" → Access Analyzer policy generation (CloudTrail-based)',
      'Do NOT confuse with Macie (which detects PII in S3, not access)',
    ],
    awsServices: ['IAM Access Analyzer', 'CloudTrail', 'Security Hub'],
    confusedWith: ['iam-fundamentals', 'macie', 'security-hub'],
  },
  {
    slug: 'iam-permissions-boundaries',
    name: 'IAM Permissions Boundaries',
    description: 'Advanced feature that establishes the MAXIMUM permission an identity (user/role) can have, regardless of its associated policies',
    domainSlug: 'secure-architecture',
    topicSlug: 'iam-identity',
    difficulty: 0.7,
    keyFacts: [
      'Permissions Boundary = ceiling: the identity only has permissions present in BOTH (associated policy AND boundary)',
      'Useful for delegation: give devs the ability to create roles WITHOUT them being able to escalate privileges',
      'Does not grant permissions on its own — only limits. Without an associated policy, an identity with a boundary cannot do anything',
      'Effective permissions = Identity policies ∩ Permissions Boundary ∩ (no SCP DENY) ∩ (no ACL DENY)',
      'Different from SCPs: SCP applies to a whole account/OU, Permissions Boundary applies to a specific identity',
      'Principal use case: "developer can create roles but only with permissions <= X"',
    ],
    examTips: [
      '"Allow developer to create IAM roles but prevent privilege escalation" → Permissions Boundary',
      '"Set organization-wide maximum permissions" → SCP (NOT Permissions Boundary)',
      '"Individual user must not be able to exceed X permissions regardless of policies" → Permissions Boundary',
      'If asking "max in account" → SCP; if "max in specific user/role" → Boundary',
    ],
    awsServices: ['IAM', 'AWS Organizations'],
    confusedWith: ['scp-organizations', 'iam-fundamentals'],
  },
  {
    slug: 'directory-service',
    name: 'AWS Directory Service',
    description: 'Managed directory services: AWS Managed Microsoft AD, AD Connector, Simple AD — when to use each',
    domainSlug: 'secure-architecture',
    topicSlug: 'iam-identity',
    difficulty: 0.55,
    keyFacts: [
      'AWS Managed Microsoft AD: real AD managed in AWS — supports trusts with on-prem AD, Group Policy, integrates with AD-dependent apps',
      'AD Connector: proxy to your existing on-prem AD — useful when you do not want to replicate users to the cloud',
      'Simple AD (Samba-based): cheap but limited alternative — no trusts, no advanced group policy. Legacy',
      'Managed AD use cases: Amazon WorkSpaces with AD, RDS SQL Server with Windows Authentication, EC2 Windows joined to domain',
      'Integrates with IAM Identity Center for SSO over AWS apps',
      'Multi-region replication (Managed AD): global directory with replication to multiple regions',
    ],
    examTips: [
      '"Need full AD compatibility for legacy app" → Managed Microsoft AD',
      '"Users exist in on-prem AD, don\'t want to replicate" → AD Connector',
      '"RDS SQL Server with Windows auth" → Managed Microsoft AD',
      '"Join EC2 Windows to corporate domain seamlessly" → Managed AD or AD Connector',
    ],
    awsServices: ['Directory Service', 'Managed Microsoft AD', 'AD Connector', 'WorkSpaces'],
    confusedWith: ['iam-identity-center-sso', 'cognito'],
  },
  {
    slug: 'macie',
    name: 'Amazon Macie',
    description: 'Sensitive data discovery service (PII, PHI, credentials) in S3 using ML and pattern matching',
    domainSlug: 'secure-architecture',
    topicSlug: 'data-protection',
    difficulty: 0.45,
    keyFacts: [
      'Scans S3 for: PII (names, emails, SSN, credit cards), PHI (HIPAA), credentials (API keys, private keys)',
      'Managed data identifiers (built-in) + custom data identifiers (regex you define)',
      'Alerts integrated with EventBridge + Security Hub — automate response (quarantine, compliance alert)',
      'Multi-account: enabled from Organizations management account',
      'Billing: per GB analyzed + per object inventoried — uses sensible sampling for large scans',
      'Does NOT attack public buckets on its own: combine with IAM Access Analyzer + AWS Config',
    ],
    examTips: [
      '"Detect PII stored in S3" → Macie (NOT GuardDuty)',
      '"HIPAA / GDPR compliance audit of S3 data" → Macie',
      '"Find exposed credentials in S3" → Macie (use custom identifiers if they are proprietary formats)',
      'If the question is about THREAT detection (compromised instances) → GuardDuty, NOT Macie',
    ],
    awsServices: ['Macie', 'Security Hub', 'EventBridge'],
    confusedWith: ['guardduty', 'iam-access-analyzer'],
  },
  {
    slug: 's3-object-lock',
    name: 'S3 Object Lock and Retention Modes',
    description: 'Protect S3 objects against deletion/overwrite with WORM: Governance vs Compliance mode, Legal Hold',
    domainSlug: 'secure-architecture',
    topicSlug: 'data-protection',
    difficulty: 0.55,
    keyFacts: [
      'Requires versioning enabled and must be enabled when creating the bucket (no retrofitting later)',
      'Governance mode: users with special permission (s3:BypassGovernanceRetention) can delete before retention',
      'Compliance mode: NOBODY can delete before retention — not even the root user, nor reduce the retention',
      'Legal Hold: blocks the object indefinitely until explicitly removed (independent of retention date)',
      'Meets SEC 17a-4(f), FINRA, CFTC — assessed by Cohasset Associates',
      'Applied per object version — overwriting creates a new version without lock (unless default is configured)',
    ],
    examTips: [
      '"Immutable storage for 7 years for SEC compliance" → Object Lock Compliance mode',
      '"Prevent deletion but allow admin override for emergencies" → Governance mode',
      '"Lock during legal investigation, indefinite duration" → Legal Hold',
      '"Ransomware protection for S3" → Versioning + MFA Delete + Object Lock + cross-account backup',
    ],
    awsServices: ['S3', 'S3 Versioning', 'KMS'],
    confusedWith: ['s3-replication', 'aws-backup'],
  },
  {
    slug: 's3-access-points',
    name: 'S3 Access Points and Multi-Region Access Points',
    description: 'Endpoints with workload-specific policies, and MRAP for failover/latency routing multi-region',
    domainSlug: 'secure-architecture',
    topicSlug: 'data-protection',
    difficulty: 0.5,
    keyFacts: [
      'Access Point: single endpoint (DNS) with its own policy — simplifies management of complex bucket policies',
      'Each Access Point has its own name, network origin (internet/VPC-only), and policy',
      'VPC-only access points: the bucket is only accessible from a specific VPC',
      'Multi-Region Access Points (MRAP): single global endpoint → automatic routing to the nearest region via Global Accelerator',
      'MRAP supports failover (2 minutes) when a region is unavailable',
      'Object Lambda Access Points: transform objects on read (e.g. PII redaction on GET) — Lambda in the path',
    ],
    examTips: [
      '"Multiple applications with different access patterns to same bucket" → Access Points',
      '"Global app with users in multiple regions accessing same data" → Multi-Region Access Point',
      '"Transform / redact data on-the-fly when downloading from S3" → Object Lambda Access Point',
      '"Restrict S3 access to specific VPC only" → VPC-only Access Point',
    ],
    awsServices: ['S3', 'S3 Access Points', 'Global Accelerator', 'Lambda'],
    confusedWith: ['s3-security', 'vpc-endpoints'],
  },
  {
    slug: 'inspector',
    name: 'Amazon Inspector',
    description: 'Continuous vulnerability assessment for EC2, container images (ECR), and Lambda',
    domainSlug: 'secure-architecture',
    topicSlug: 'network-security',
    difficulty: 0.45,
    keyFacts: [
      'Scans EC2 (CVEs in OS packages), ECR images (before deploy), Lambda (code and dependencies)',
      'Continuously updated CVE database + AWS knowledge network — findings prioritized with CVSS',
      'Automatic: scans every change (new EC2, new image push to ECR, new Lambda version)',
      'Integrated with Security Hub and EventBridge for alerts and automated remediation',
      'Replaces Inspector Classic (v1) which required manual configuration and agents — v2 uses SSM Agent',
      'Multi-account via AWS Organizations: central view of vulnerabilities',
    ],
    examTips: [
      '"Detect CVEs in EC2 instances" → Inspector',
      '"Scan container images before deployment" → Inspector (ECR integration)',
      '"Find vulnerable dependencies in Lambda code" → Inspector',
      'Do NOT confuse with GuardDuty (runtime threat detection) or Macie (PII in S3)',
    ],
    awsServices: ['Inspector', 'ECR', 'Lambda', 'EC2', 'Security Hub'],
    confusedWith: ['guardduty', 'macie', 'security-hub'],
  },
  // ---- Cost extras ----
  {
    slug: 'compute-optimizer',
    name: 'AWS Compute Optimizer',
    description: 'ML-based rightsizing recommendations for EC2, Auto Scaling groups, EBS, Lambda, and ECS Fargate',
    domainSlug: 'cost-optimized-architecture',
    topicSlug: 'cost-compute',
    difficulty: 0.4,
    keyFacts: [
      'Analyzes 14 days (default) of CloudWatch metrics — recommends instance types with better price/performance',
      'Covers: EC2, Auto Scaling groups, EBS volumes, Lambda functions, ECS Fargate services, RDS',
      'Enhanced Infrastructure Metrics ($$): analysis up to 3 months and memory metrics (requires CloudWatch Agent)',
      'Findings: Under-provisioned, Over-provisioned, Optimized — with estimated savings',
      'Free for basic recommendations; Enhanced has cost per resource',
      'Integrable with Organizations for central multi-account view',
    ],
    examTips: [
      '"Identify over-provisioned EC2 instances" → Compute Optimizer',
      '"Right-size Lambda memory allocation" → Compute Optimizer (NOT manual Power Tuning for large fleet)',
      '"Recommend EBS volume type change" → Compute Optimizer (e.g. gp2 → gp3)',
      '"Cost recommendations across the whole org" → Compute Optimizer + Organizations',
    ],
    awsServices: ['Compute Optimizer', 'CloudWatch', 'EC2', 'Lambda'],
    confusedWith: ['trusted-advisor', 'cost-optimization-strategies'],
  },
  {
    slug: 'savings-plans-strategy',
    name: 'Savings Plans: Compute vs EC2 Instance vs SageMaker',
    description: 'Types of Savings Plans, how they compare to Reserved Instances, and how to combine for maximum savings',
    domainSlug: 'cost-optimized-architecture',
    topicSlug: 'cost-compute',
    difficulty: 0.65,
    keyFacts: [
      'Compute Savings Plans: up to 66% discount — applies to EC2, Fargate, and Lambda, any region/family/OS',
      'EC2 Instance Savings Plans: up to 72% discount — region and family specific, but allows changing size within the family',
      'SageMaker Savings Plans: discounts for training + inference on SageMaker',
      'Commitment: $/hour for 1 or 3 years, all-upfront / partial / no-upfront (more upfront = more discount)',
      'Savings Plans are applied BEFORE Reserved Instances — if you have both, RI first, then SP applies to the remaining',
      'Leftovers never expire: if you consume less than the commitment, you pay the difference',
    ],
    examTips: [
      '"Maximum flexibility across compute services" → Compute Savings Plans',
      '"Predictable EC2 family usage, maximum discount" → EC2 Instance Savings Plans',
      '"Migrate between instance types within family (e.g. m5 → m6i)" → EC2 Instance SP allows it, RI Standard does not',
      '"Committing without knowing exact instance types" → Compute SP (more flexible than RI)',
    ],
    awsServices: ['Savings Plans', 'EC2', 'Fargate', 'Lambda', 'SageMaker'],
    confusedWith: ['ec2-pricing-models', 'compute-optimizer'],
  },
  {
    slug: 'ebs-cost-optimization',
    name: 'EBS: Cost Optimization',
    description: 'Strategies to reduce EBS cost: gp2→gp3 migration, snapshot lifecycle, archive tier, unattached volumes',
    domainSlug: 'cost-optimized-architecture',
    topicSlug: 'cost-storage',
    difficulty: 0.5,
    keyFacts: [
      'gp3 is 20% cheaper than gp2 with the same or better performance — hot migration with no downtime',
      'Snapshots only charge for delta vs the previous snapshot — but they accumulate. Data Lifecycle Manager manages them',
      'EBS Snapshot Archive (2022+): long-term snapshots with ~75% lower cost, retrieval 24-72h',
      'Recycle Bin: configurable retention for deleted snapshots/AMIs — prevents accidental loss without paying for active EBS',
      'Unattached volumes: are still billed — Trusted Advisor or Compute Optimizer identify them',
      'io2 vs io1: io2 is the same price but 100x better durability — always prefer io2',
    ],
    examTips: [
      '"Reduce EBS costs with no performance impact" → migrate gp2 → gp3',
      '"Keep snapshots for compliance 7 years cheaply" → Snapshot Archive + Lifecycle Manager',
      '"Delete old snapshots automatically" → Data Lifecycle Manager',
      '"Recover accidentally deleted snapshot" → Recycle Bin (if it was enabled)',
    ],
    awsServices: ['EBS', 'Data Lifecycle Manager', 'Recycle Bin', 'Trusted Advisor'],
    confusedWith: ['ebs-volume-types', 's3-lifecycle'],
  },
  {
    slug: 'storage-tier-decision-matrix',
    name: 'Decision matrix: S3 vs EBS vs EFS vs Glacier',
    description: 'Framework for choosing the correct tier/service based on access latency, access pattern, size, and retrieval time',
    domainSlug: 'cost-optimized-architecture',
    topicSlug: 'cost-storage',
    difficulty: 0.55,
    keyFacts: [
      'S3 Standard: frequent access (>1/month), ms latency, $0.023/GB — web assets, active data lakes',
      'S3 Standard-IA: infrequent access (<1/month) but ms retrieval, $0.0125/GB + retrieval fee',
      'S3 Glacier Instant: quarterly access, ms retrieval, $0.004/GB — best option for archive-but-sometimes-needed',
      'S3 Glacier Flexible: minutes-hours retrieval, $0.0036/GB — quarterly/annual backups',
      'S3 Glacier Deep Archive: 12h retrieval, $0.00099/GB — alternative to tape for 7-10 year compliance',
      'EBS: ~$0.08/GB gp3 — only to attach to EC2, NOT shareable (except io1/io2 multi-attach)',
      'EFS Standard: $0.30/GB — only if you need shared NFS; lifecycle to IA/Archive reduces drastically',
    ],
    examTips: [
      '"Unknown access pattern" → S3 Intelligent-Tiering',
      '"Archive but need fast access occasionally" → S3 Glacier Instant Retrieval',
      '"Cheapest archive, 12h retrieval OK" → S3 Glacier Deep Archive',
      '"Shared filesystem for EC2 fleet" → EFS (Linux) or FSx (Windows/HPC)',
      '"Block storage for single EC2" → EBS gp3 default',
    ],
    awsServices: ['S3', 'EBS', 'EFS', 'Glacier'],
    confusedWith: ['s3-storage-classes', 's3-intelligent-tiering', 'ebs-volume-types'],
  },
  {
    slug: 'aws-budgets-cost-explorer',
    name: 'AWS Budgets and Cost Explorer',
    description: 'Cost visibility and control tools: Cost Explorer for analysis, Budgets for proactive alerts/actions',
    domainSlug: 'cost-optimized-architecture',
    topicSlug: 'cost-governance',
    difficulty: 0.4,
    keyFacts: [
      'Cost Explorer: UI and API to explore costs by service/tag/account, up to 12 historical months + 12 forecast',
      'Savings Plans recommendations and RI recommendations generated by Cost Explorer',
      'Budgets: alerts when ACTUAL or FORECAST cost exceeds threshold — via email/SNS',
      'Budget Actions (2020+): automated response — apply SCP that denies RunInstances, stop EC2, remove permissions',
      'Budget types: cost, usage, RI utilization/coverage, Savings Plans utilization/coverage',
      'Cost Allocation Tags: enabled in Billing → enable breakdown in Cost Explorer and Budgets',
    ],
    examTips: [
      '"Alert when monthly spend exceeds $X" → AWS Budgets',
      '"Automatically stop EC2 when budget threshold hit" → Budget Actions',
      '"Visualize costs by team using tags" → Cost Allocation Tags + Cost Explorer',
      '"Forecast next month\'s spend" → Cost Explorer forecast',
    ],
    awsServices: ['AWS Budgets', 'Cost Explorer', 'SNS', 'IAM'],
    confusedWith: ['trusted-advisor', 'cost-optimization-strategies'],
  },
  {
    slug: 'tag-policies',
    name: 'Tag Policies and Cost Allocation Strategy',
    description: 'Tag governance at the Organization level: tag policies, cost allocation tags, chargeback to teams',
    domainSlug: 'cost-optimized-architecture',
    topicSlug: 'cost-governance',
    difficulty: 0.5,
    keyFacts: [
      'Tag Policies (in AWS Organizations): enforce case + allowed values for specific keys (e.g. Environment: prod|staging|dev)',
      'Tag Policies on their own do NOT block — they report non-compliance in the Resource Groups Tagging API',
      'For hard-enforce: combine Tag Policy with an SCP like "Deny RunInstances if tag not present"',
      'Cost Allocation Tags: enable them in Billing console so they appear in Cost Explorer and CUR',
      'User-defined tags + AWS-generated tags (createdBy) — both can be enabled',
      'Chargeback pattern: Tag Policy enforce + Cost Categories grouping by tag → Cost Explorer report by team',
    ],
    examTips: [
      '"Enforce Environment tag across org" → Tag Policy + SCP (Policy alone does not block)',
      '"Chargeback cost to teams" → Cost Allocation Tags + Cost Categories',
      '"Identify untagged resources" → Resource Groups Tagging API + Tag Policy report',
    ],
    awsServices: ['AWS Organizations', 'Tag Policies', 'SCP', 'Cost Explorer'],
    confusedWith: ['scp-organizations', 'cost-optimization-strategies'],
  },
  {
    slug: 'fargate-spot',
    name: 'Fargate Spot and ECS Capacity Providers',
    description: 'Container savings: Fargate Spot up to 70% off, mixed capacity providers with On-Demand + Spot',
    domainSlug: 'cost-optimized-architecture',
    topicSlug: 'cost-compute',
    difficulty: 0.55,
    keyFacts: [
      'Fargate Spot: up to 70% discount vs Fargate On-Demand — can be interrupted with 2min notice',
      'Only available for ECS (Fargate); EKS Fargate does NOT support Spot (but does support EC2 Spot with EKS)',
      'Capacity Providers in ECS: mix ON-DEMAND + SPOT with base + weight ratio (e.g. 1 base On-Demand + 3:1 Spot)',
      'Task interruption handling: ECS sends SIGTERM, 2 min for graceful shutdown',
      'For stateless workloads, batch, CI/CD, dev/test environments — NOT for persistent databases',
      'EKS with EC2 Spot: managed node groups support mixed On-Demand + Spot directly',
    ],
    examTips: [
      '"Reduce Fargate costs for batch processing" → Fargate Spot',
      '"Stateful service that must not be interrupted" → NOT Spot',
      '"Mix On-Demand for baseline + Spot for burst in ECS" → ECS Capacity Providers with both',
      '"Cost-optimize EKS compute" → EC2 Spot in managed node group (Fargate Spot not supported on EKS)',
    ],
    awsServices: ['ECS', 'Fargate', 'Fargate Spot', 'EKS'],
    confusedWith: ['spot-fleet-strategies', 'ecs-vs-eks-vs-fargate'],
  },

  // ---- Data Analytics & Lake (Domain 3, task 3.5) ----
  {
    slug: 'athena-fundamentals',
    name: 'Amazon Athena',
    description: 'Serverless ad-hoc SQL service over data in S3 — pay per TB scanned, no cluster to manage',
    domainSlug: 'performant-architecture',
    topicSlug: 'data-analytics',
    difficulty: 0.5,
    keyFacts: [
      'Serverless — no clusters; pay per TB scanned, not by time',
      'Supports CSV, JSON, Parquet, ORC, Avro; Parquet/ORC drastically reduce cost (column pruning)',
      'Partitioning by common columns (date, region) reduces scanned data → less cost',
      'Uses the AWS Glue Data Catalog as default metastore',
      'Federated queries allow querying RDS/DynamoDB/Redshift from Athena',
      'Workgroups isolate queries by team and apply cost/performance limits',
    ],
    examTips: [
      '"Ad-hoc SQL over logs in S3 without managing infra" → Athena',
      '"Reduce Athena query cost" → convert to Parquet + partition',
      'Athena ≠ Redshift Spectrum: Athena is 100% serverless; Spectrum requires a Redshift cluster',
    ],
    awsServices: ['Athena', 'Glue Data Catalog', 'S3'],
    confusedWith: ['redshift-fundamentals', 'glue-catalog-etl'],
  },
  {
    slug: 'glue-catalog-etl',
    name: 'AWS Glue (Catalog + ETL)',
    description: 'Managed service for data lake metastore (Data Catalog) and ETL jobs (serverless Spark or Python shell)',
    domainSlug: 'performant-architecture',
    topicSlug: 'data-analytics',
    difficulty: 0.55,
    keyFacts: [
      'Glue Data Catalog: central metastore — tables, partitions, schemas — used by Athena, EMR, Redshift Spectrum, Lake Formation',
      'Glue Crawlers: automatically discover schemas over S3, RDS, DynamoDB',
      'Glue ETL Jobs: serverless Spark (DynamicFrames) or Python shell — pay per DPU-hour',
      'Glue Studio: code-free visual editor; Glue DataBrew: code-free visual cleansing',
      'Glue Streaming: ETL over Kinesis/MSK with micro-batches',
      'Worker types: G.1X, G.2X (standard), G.025X (small jobs), Z.2X (memory intensive)',
    ],
    examTips: [
      '"Central catalog of schemas for data lake" → Glue Data Catalog',
      '"Serverless ETL without managing a Spark cluster" → Glue ETL',
      '"Schema auto-discovery in S3" → Glue Crawler',
    ],
    awsServices: ['Glue', 'Glue Data Catalog', 'Glue Crawlers', 'S3'],
    confusedWith: ['athena-fundamentals', 'emr-overview'],
  },
  {
    slug: 'lake-formation-governance',
    name: 'AWS Lake Formation',
    description: 'Governance and security layer over the S3 data lake — centralized permissions at column, row, and cell level',
    domainSlug: 'performant-architecture',
    topicSlug: 'data-analytics',
    difficulty: 0.6,
    keyFacts: [
      'Built on top of Glue Data Catalog — adds fine-grained permissions on tables/columns/rows',
      'GRANT/REVOKE permissions in SQL style instead of IAM JSON (much simpler)',
      'Cross-account data sharing without replicating data — uses Resource Links',
      'LF-Tags (tag-based access control): apply policies by tag instead of by resource',
      'Row-level and cell-level filtering: data filters at row/column level by user or role',
      'Centralized audit of data lake accesses in CloudTrail',
    ],
    examTips: [
      '"Row-level security in data lake" → Lake Formation data filters',
      '"Share data across accounts without copying" → Lake Formation cross-account + Resource Links',
      '"Centralize data lake permissions" → Lake Formation (not pure IAM)',
    ],
    awsServices: ['Lake Formation', 'Glue Data Catalog', 'S3', 'IAM'],
    confusedWith: ['glue-catalog-etl', 'iam-permissions-boundaries'],
  },
  {
    slug: 'emr-overview',
    name: 'Amazon EMR',
    description: 'Managed Hadoop/Spark/Hive/Presto/HBase cluster — for massive big data processing',
    domainSlug: 'performant-architecture',
    topicSlug: 'data-analytics',
    difficulty: 0.55,
    keyFacts: [
      'Frameworks: Spark, Hadoop, Hive, Presto, HBase, Flink, Trino — pre-installed',
      'Cluster modes: long-running, transient (job-specific), EMR Serverless, EMR on EKS, EMR on Outposts',
      'Master + Core (HDFS) + Task (compute-only, ideal Spot) — Task nodes are the cheap ones',
      'EMRFS: HDFS over S3 — decouples compute from storage; allows transient clusters',
      'EMR Serverless: no node management; pay per vCPU/memory/second',
      'For Spark/Hadoop with custom code; Athena/Glue are simpler serverless alternatives for basic SQL/ETL',
    ],
    examTips: [
      '"Process petabytes with Spark with custom code" → EMR (or EMR Serverless)',
      '"Managed Hadoop ecosystem" → EMR',
      '"Reduce EMR cost" → Task nodes on Spot + transient clusters + EMRFS',
    ],
    awsServices: ['EMR', 'EMR Serverless', 'EMR on EKS', 'S3', 'Glue Catalog'],
    confusedWith: ['athena-fundamentals', 'glue-catalog-etl'],
  },
  {
    slug: 'quicksight-overview',
    name: 'Amazon QuickSight',
    description: 'Serverless BI service with SPICE in-memory engine — dashboards and embedded analytics',
    domainSlug: 'performant-architecture',
    topicSlug: 'data-analytics',
    difficulty: 0.45,
    keyFacts: [
      'SPICE engine: in-memory columnar for sub-second queries over millions of rows',
      'Native connectors: RDS, Redshift, Athena, S3, OpenSearch, Snowflake, Salesforce, ...',
      'Standard Edition vs Enterprise (SSO, row-level security, VPC, ML insights, embedded)',
      'Pay per user (Authors) + Readers (session-based) + SPICE capacity',
      'QuickSight Q: NLP — natural-language questions over data',
      'Embedded analytics: integrate dashboards into apps via SDK',
    ],
    examTips: [
      '"Managed serverless BI with embedding" → QuickSight (not Tableau/PowerBI)',
      '"Sub-second dashboards on big data" → QuickSight with SPICE',
      '"Row-level security in BI" → QuickSight Enterprise + RLS',
    ],
    awsServices: ['QuickSight', 'Athena', 'Redshift', 'S3'],
    confusedWith: ['athena-fundamentals', 'redshift-fundamentals'],
  },
]

export function getConceptBySlug(slug: string): ConceptDefinition | undefined {
  return CONCEPTS.find(c => c.slug === slug)
}

export function getConceptsByDomain(domainSlug: string): ConceptDefinition[] {
  return CONCEPTS.filter(c => c.domainSlug === domainSlug)
}

export function getDomainBySlug(slug: string): DomainDefinition | undefined {
  return DOMAINS.find(d => d.slug === slug)
}

export function getTopicsByDomain(domainSlug: string): TopicDefinition[] {
  return TOPICS.filter(t => t.domainSlug === domainSlug)
}
