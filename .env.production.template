# ============================================
# MAESTRING — Vercel Production Environment
# ============================================
# Copy-paste this entire block into Vercel:
#   Project Settings → Environment Variables → "Import .env" or paste raw
# Set Environment to: Production (and Preview if you want previews to work too)
#
# Replace every TODO_* before importing. Items already filled are safe to use.
# ============================================

# -- App URLs (PRODUCTION) --
NEXT_PUBLIC_APP_URL=https://maestring.com
NEXT_PUBLIC_SITE_URL=https://maestring.com

# -- Supabase (PRODUCTION project — NOT local) --
# Create the project at supabase.com → New project → region eu-west-1 (Ireland)
# Copy from Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=TODO_https://xxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=TODO_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=TODO_supabase_service_role_key

# -- AI --
ANTHROPIC_API_KEY=TODO_sk-ant-...
OPENAI_API_KEY=TODO_sk-...
OPENAI_CHAT_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# -- Stripe (LIVE mode for prod) --
# Get from dashboard.stripe.com → Developers → API keys (toggle "View live data")
STRIPE_SECRET_KEY=TODO_sk_live_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=TODO_pk_live_...
# Webhook secret: created later (see DEPLOY.md step 6)
STRIPE_WEBHOOK_SECRET=TODO_whsec_...
# Price IDs from Stripe → Products → tu producto Pro
STRIPE_PRICE_PRO_MONTHLY=TODO_price_...
STRIPE_PRICE_PRO_ANNUAL=TODO_price_...

# -- Upstash Redis (rate limiting) --
# Create at console.upstash.com → New database → Global → eu-west-1
UPSTASH_REDIS_REST_URL=TODO_https://xxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=TODO_AY...

# -- Resend --
# Create API key at resend.com → API Keys → "Sending access" only
RESEND_API_KEY=TODO_re_...
RESEND_FROM_EMAIL=no-reply@maestring.com
RESEND_FROM_NAME=Maestring

# -- PostHog (analytics) --
# Create project at app.posthog.com → Project API Key
NEXT_PUBLIC_POSTHOG_KEY=TODO_phc_...
NEXT_PUBLIC_POSTHOG_HOST=https://eu.posthog.com

# -- Sentry (errors) --
NEXT_PUBLIC_SENTRY_DSN=TODO_https://xxx@sentry.io/xxx

# -- Auth / admin --
ADMIN_EMAILS=aleopenclaw@gmail.com

# -- Cron + magic links (already generated, safe to use as-is) --
CRON_SECRET=cb3856984ac08c2fa5b3f29f47b46263906f313a90f6eebe39066f3d64c84fa4
MAGIC_LINK_SECRET=15f275d57f769194e742ec40e35313930f60fd310008fbd9ab9caf74ae27cb1a
